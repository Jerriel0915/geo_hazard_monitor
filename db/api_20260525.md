# 地质灾害监测预警系统 - 后端接口设计文档

**日期：** 2026-06-03\
**版本：** v1.5\
**作者：** 系统开发组

***

## 1. 接口基础规范

### 1.1 请求方式

| 操作 | HTTP方法 | 示例                           |
| -- | ------ | ---------------------------- |
| 查询 | GET    | `/api/v1/hazard-points/page` |
| 新增 | POST   | `/api/v1/hazard-points`      |
| 修改 | PUT    | `/api/v1/hazard-points/{id}` |
| 删除 | DELETE | `/api/v1/hazard-points/{id}` |

### 1.2 路径规范

- 小写 + 中横线
- 按模块划分：`/api/v1/{module}/{resource}`

### 1.3 公共请求头

```http
Authorization: Bearer {token}
Content-Type: application/json
```

### 1.4 统一返回格式

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {},
  "timestamp": 1735689213000
}
```

### 1.5 状态码定义

| 状态码 | 含义    | 说明          |
| --- | ----- | ----------- |
| 200 | 成功    | 操作成功        |
| 400 | 参数错误  | 参数缺失、校验失败、类型不匹配、JSON解析失败 |
| 401 | 未登录   | Token过期或未登录 |
| 403 | 无权限   | 没有操作权限      |
| 404 | 不存在   | 资源不存在       |
| 405 | 方法不允许 | 请求方法不支持    |
| 409 | 冲突    | 幂等请求冲突、唯一键冲突、状态不一致 |
| 415 | 媒体类型错误 | Content-Type 不受支持 |
| 500 | 服务器异常 | 系统内部错误      |
| 601 | 预警触发  | 告警触发        |
| 602 | 设备离线  | 设备离线通知      |

### 1.5.1 常见异常响应

| 场景 | 返回码 | 典型消息 |
| --- | --- | --- |
| 缺少必填查询参数 | 400 | `缺少必填参数[id]` |
| 路径/查询参数类型不匹配 | 400 | `请求参数类型不匹配，参数[id]要求类型为：'java.lang.Long'...` |
| `@RequestBody` 校验失败 | 400 | `用户名不能为空` |
| 方法级参数校验失败 | 400 | `请求参数校验失败` 或具体校验消息 |
| JSON 格式错误或请求体为空 | 400 | `请求体格式错误或JSON解析失败` |
| 请求方法不支持 | 405 | `Request method 'POST' is not supported` |
| 请求内容类型不支持 | 415 | `不支持的请求内容类型` |
| 接口或静态资源不存在 | 404 | `请求资源不存在` |
| 未登录或 Token 无效 | 401 | `请求访问：{uri}，认证失败，无法访问系统资源` |
| 无接口权限 | 403 | `没有权限，请联系管理员授权` |

### 1.6 分页规范

**请求参数：**

- `pageNum`: 页码，默认1
- `pageSize`: 每页数量，默认10

**返回格式：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 128,
    "rows": [],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

***

## 2. 系统管理模块接口

### 2.1 用户认证

**说明：**

- 登录、验证码、用户信息、路由接口统一使用 `/api/v1/auth` 前缀。
- 登出接口由 Spring Security 接管，实际路径为 `/api/v1/system/auth/logout`。
- 注册接口为独立匿名接口 `/register`。

#### 2.1.1 获取验证码

**路径：** `GET /api/v1/auth/captcha`\
**描述：** 获取登录/注册验证码。当前系统默认验证码类型为 `math`，但接口返回的是 Base64 图片，不直接返回算式文本。

**请求参数：** 无

**成功响应（验证码开启）：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "captchaEnabled": true,
    "captchaKey": "6f4a94b3d7b24f0e8f15f28f2a6b32fd",
    "captchaImage": "/9j/4AAQSkZJRgABAQAAAQABAAD..."
  },
  "timestamp": 1779624887000
}
```

**成功响应（验证码关闭）：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "captchaEnabled": false
  },
  "timestamp": 1779624887000
}
```

| 字段           | 类型      | 必返 | 说明 |
| ------------ | ------- | -- | -- |
| captchaEnabled | boolean | 是  | 是否启用验证码 |
| captchaKey   | string  | 否  | 验证码唯一标识，验证码开启时返回 |
| captchaImage | string  | 否  | Base64 编码图片内容，前端可拼接 `data:image/png;base64,` 使用 |

#### 2.1.2 登录

**路径：** `POST /api/v1/auth/login`\
**描述：** 用户名密码登录，成功后返回 JWT Token。

**请求体：**

```json
{
  "username": "admin",
  "password": "admin123",
  "code": "1234",
  "uuid": "6f4a94b3d7b24f0e8f15f28f2a6b32fd",
  "rememberMe": false
}
```

| 字段       | 类型      | 必填 | 说明 |
| -------- | ------- | -- | -- |
| username | string  | 是  | 登录用户名 |
| password | string  | 是  | 登录密码 |
| code     | string  | 否  | 验证码内容；当 `captchaEnabled=true` 时必填 |
| uuid     | string  | 否  | 验证码标识；当 `captchaEnabled=true` 时必填，对应验证码接口返回的 `captchaKey` |
| rememberMe | boolean | 否  | 是否记住我，默认 `false`；`true` 时 Token 有效期为 7 天，否则为 20 分钟 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "登陆成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9.eyJsb2dpbl91c2VyX2tleSI6IjYyNTYyZjcxLTAxN2ItNDM5MS1hZmEyLTlkZjRkNzYxM2M3OSIsImp3dF91c2VybmFtZSI6ImFkbWluIn0.zJx...",
    "expiresIn": 1200
  },
  "timestamp": 1779624887000
}
```

| 字段      | 类型     | 必返 | 说明 |
| ------- | ------ | -- | -- |
| token   | string | 是  | JWT Token，后续请求需在请求头中携带 `Authorization: Bearer {token}` |
| expiresIn | int    | 是  | Token 过期时间，单位秒；普通登录为 `1200`，记住我登录为 `604800` |

#### 2.1.3 获取当前登录用户信息

**路径：** `GET /api/v1/auth/getInfo`\
**描述：** 获取当前登录用户详情、角色、权限以及密码策略相关信息。

**请求头：**

| 参数            | 类型     | 必填 | 说明 |
| ------------- | ------ | -- | -- |
| Authorization | string | 是  | Bearer Token |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "user": {
      "userId": 1,
      "deptId": 103,
      "userName": "admin",
      "nickName": "系统管理员",
      "email": "admin@example.com",
      "phonenumber": "13800138000",
      "sex": "0",
      "avatar": "",
      "status": "0",
      "loginIp": "127.0.0.1",
      "loginDate": "2026-05-24 10:20:30",
      "dept": {
        "deptId": 103,
        "parentId": 100,
        "deptName": "监测中心"
      }
    },
    "roles": [
      "admin"
    ],
    "permissions": [
      "*:*:*"
    ],
    "pwdChrtype": "0",
    "isDefaultModifyPwd": false,
    "isPasswordExpired": false
  },
  "timestamp": 1779624887000
}
```

| 字段                 | 类型           | 必返 | 说明 |
| ------------------ | ------------ | -- | -- |
| user               | object       | 是  | 当前登录用户对象，实际结构遵循后端 `SysUser` 序列化结果 |
| roles              | string[]     | 是  | 当前用户角色标识集合 |
| permissions        | string[]     | 是  | 当前用户权限标识集合 |
| pwdChrtype         | string       | 是  | 密码字符规则配置值，对应系统配置 `sys.account.chrtype` |
| isDefaultModifyPwd | boolean      | 是  | 是否仍在使用初始密码且需提醒修改 |
| isPasswordExpired  | boolean      | 是  | 当前密码是否已过期 |

#### 2.1.4 获取当前用户路由

**路径：** `GET /api/v1/auth/getRouters`\
**描述：** 获取当前登录用户可访问的前端动态路由树。

**请求头：**

| 参数            | 类型     | 必填 | 说明 |
| ------------- | ------ | -- | -- |
| Authorization | string | 是  | Bearer Token |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": [
    {
      "name": "System",
      "path": "/system",
      "hidden": false,
      "redirect": "noRedirect",
      "component": "Layout",
      "alwaysShow": true,
      "meta": {
        "title": "系统管理",
        "icon": "system",
        "noCache": true
      },
      "children": [
        {
          "name": "User",
          "path": "user",
          "hidden": false,
          "component": "system/user/index",
          "query": "",
          "meta": {
            "title": "用户管理",
            "icon": "user",
            "noCache": true
          }
        }
      ]
    }
  ],
  "timestamp": 1779624887000
}
```

**返回字段说明：**

| 字段         | 类型        | 必返 | 说明 |
| ---------- | --------- | -- | -- |
| name       | string    | 否  | 路由名称 |
| path       | string    | 是  | 路由路径 |
| hidden     | boolean   | 否  | 是否在侧边栏隐藏 |
| redirect   | string    | 否  | 重定向地址 |
| component  | string    | 否  | 前端组件路径 |
| query      | string    | 否  | 路由参数字符串 |
| alwaysShow | boolean   | 否  | 是否始终显示根路由 |
| meta       | object    | 否  | 路由元信息，包含 `title`、`icon`、`noCache`、`link` |
| children   | object[]  | 否  | 子路由列表 |

#### 2.1.5 登出

**路径：** `POST /api/v1/system/auth/logout`\
**描述：** 当前用户退出登录并删除 Redis 中的登录态缓存。该接口由 Spring Security 统一处理，不经过业务 Controller。

**请求头：**

| 参数            | 类型     | 必填 | 说明 |
| ------------- | ------ | -- | -- |
| Authorization | string | 是  | Bearer Token |

**请求体：** 无

**成功响应：**

```json
{
  "code": 200,
  "msg": "退出成功",
  "timestamp": 1779624887000
}
```

#### 2.1.6 用户注册

**路径：** `POST /register`\
**描述：** 匿名注册新用户。仅当系统配置 `sys.account.registerUser=true` 时可用；注册对象继承登录对象，因此入参结构与登录一致。

**请求体：**

```json
{
  "username": "testuser",
  "password": "123456",
  "code": "4321",
  "uuid": "6f4a94b3d7b24f0e8f15f28f2a6b32fd"
}
```

| 字段       | 类型     | 必填 | 说明 |
| -------- | ------ | -- | -- |
| username | string | 是  | 注册用户名，长度 2-20 |
| password | string | 是  | 登录密码，长度 5-20 |
| code     | string | 否  | 验证码内容；当系统开启验证码时必填 |
| uuid     | string | 否  | 验证码标识；当系统开启验证码时必填 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "timestamp": 1779624887000
}
```

**失败响应示例（系统未开启注册）：**

```json
{
  "code": 500,
  "msg": "当前系统没有开启注册功能！",
  "timestamp": 1779624887000
}
```

#### 2.1.7 个人中心

说明：

- 个人中心接口统一使用 `/api/v1/profile/*`
- 该组接口用于当前登录用户查看和维护自己的资料，不用于管理员代他人操作

##### 2.1.7.1 获取个人中心信息

**路径：** `GET /api/v1/profile`\
**描述：** 获取当前登录用户的个人中心资料。

**请求头：**

| 参数            | 类型     | 必填 | 说明 |
| ------------- | ------ | -- | -- |
| Authorization | string | 是  | Bearer Token |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "id": 1,
    "username": "admin",
    "realName": "系统管理员",
    "avatar": "/profile/avatar/2026/05/24/admin.png",
    "phone": "13800138000",
    "email": "admin@example.com",
    "orgId": 103,
    "orgName": "监测中心",
    "status": 0,
    "sex": "0",
    "roleGroup": "超级管理员",
    "postGroup": "总负责人"
  },
  "timestamp": 1779624887000
}
```

##### 2.1.7.2 修改个人中心资料

**路径：** `PUT /api/v1/profile`\
**描述：** 修改当前登录用户的个人资料。

**请求体：**

```json
{
  "realName": "系统管理员",
  "phone": "13800138001",
  "email": "admin_new@example.com",
  "sex": "0"
}
```

| 字段       | 类型     | 必填 | 说明 |
| -------- | ------ | -- | -- |
| realName | string | 是  | 真实姓名 |
| phone    | string | 否  | 手机号 |
| email    | string | 否  | 邮箱 |
| sex      | string | 否  | 性别，沿用系统字典值 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1779624887000
}
```

##### 2.1.7.3 修改个人中心密码

**路径：** `PUT /api/v1/profile/password`\
**描述：** 修改当前登录用户密码。

**请求体：**

```json
{
  "oldPassword": "old_password",
  "newPassword": "new_password"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1779624887000
}
```

##### 2.1.7.4 上传头像

**路径：** `POST /api/v1/profile/avatar`\
**描述：** 上传当前登录用户头像。

**请求类型：** `multipart/form-data`

**表单字段：**

| 字段名 | 类型   | 必填 | 说明 |
| --- | --- | --- | --- |
| file | file | 是 | 头像文件，兼容旧字段名 `avatarfile` |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "imgUrl": "/profile/avatar/2026/05/24/avatar.png",
  "timestamp": 1779624887000
}
```

##### 2.1.7.5 常见失败响应

**未登录或Token无效：**

```json
{
  "code": 401,
  "msg": "请求访问：/api/v1/profile，认证失败，无法访问系统资源",
  "timestamp": 1779624887000
}
```

**业务失败示例：**

```json
{
  "code": 500,
  "msg": "修改用户'admin'失败，手机号码已存在",
  "timestamp": 1779624887000
}
```

### 2.2 用户管理

说明：

- 用户状态：`0` 正常，`1` 停用
- 统一分页返回结构：`rows / total / pageNum / pageSize`
- 修改用户时以路径参数 `id` 为准，不依赖请求体中的 `id`

#### 2.2.0 权限要求

| 接口 | 权限标识 |
| --- | --- |
| `GET /api/v1/users/page` | `system:user:list` |
| `GET /api/v1/users/{id}` | `system:user:query` |
| `POST /api/v1/users` | `system:user:add` |
| `PUT /api/v1/users/{id}` | `system:user:edit` |
| `DELETE /api/v1/users/{id}` | `system:user:remove` |
| `DELETE /api/v1/users/batch` | `system:user:remove` |
| `PUT /api/v1/users/{id}/password` | `system:user:resetPwd` |

#### 2.2.1 分页查询用户

**路径：** `GET /api/v1/users/page`\
**描述：** 分页查询用户列表

**请求参数：**

| 参数       | 类型     | 必填 | 说明        |
| -------- | ------ | -- | --------- |
| pageNum  | int    | 否  | 页码，默认1    |
| pageSize | int    | 否  | 每页数量，默认10 |
| username | string | 否  | 用户名模糊查询   |
| realName | string | 否  | 真实姓名模糊查询  |
| orgId    | long   | 否  | 所属组织ID    |
| status   | int    | 否  | 状态        |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "rows": [
      {
        "id": 1,
        "username": "admin",
        "realName": "系统管理员",
        "avatar": "/profile/avatar/2026/05/24/admin.png",
        "phone": "13800138000",
        "email": "admin@example.com",
        "orgId": 1,
        "orgName": "监测中心",
        "status": 0,
        "lastLoginTime": "2024-01-20 14:30:00",
        "createTime": "2024-01-01 00:00:00",
        "createBy": "system",
        "updateTime": "2024-01-10 10:00:00",
        "updateBy": "admin"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 2.2.2 获取用户详情

**路径：** `GET /api/v1/users/{id}`\
**描述：** 根据ID获取用户详情

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "username": "admin",
    "realName": "系统管理员",
    "avatar": "/profile/avatar/2026/05/24/admin.png",
    "phone": "13800138000",
    "email": "admin@example.com",
    "orgId": 1,
    "orgName": "监测中心",
    "status": 0,
    "createTime": "2024-01-01 00:00:00",
    "createBy": "system",
    "updateTime": "2024-01-10 10:00:00",
    "updateBy": "admin",
    "roleIds": [1, 2],
    "postIds": [1]
  },
  "timestamp": 1735689213000
}
```

#### 2.2.3 新增用户

**路径：** `POST /api/v1/users`\
**描述：** 新增用户

**请求体：**

```json
{
  "username": "testuser",
  "password": "123456",
  "realName": "测试用户",
  "phone": "13900139000",
  "email": "test@example.com",
  "orgId": 1,
  "status": 0,
  "roleIds": [2, 3],
  "remark": "测试账号"
}
```

| 字段       | 类型        | 必填 | 说明           |
| -------- | --------- | -- | ------------ |
| username | string    | 是  | 用户名，唯一       |
| password | string    | 是  | 登录密码         |
| realName | string    | 是  | 真实姓名         |
| phone    | string    | 否  | 手机号          |
| email    | string    | 否  | 邮箱           |
| orgId    | long      | 否  | 所属组织ID       |
| status   | int       | 否  | 状态，默认建议传 `0` |
| roleIds  | long[]    | 否  | 角色ID列表       |
| remark   | string    | 否  | 备注           |

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 2
  },
  "timestamp": 1735689213000
}
```

#### 2.2.4 修改用户

**路径：** `PUT /api/v1/users/{id}`\
**描述：** 修改用户信息

**请求体：**

```json
{
  "realName": "测试用户修改",
  "phone": "13900139001",
  "email": "test_update@example.com",
  "orgId": 2,
  "status": 0,
  "roleIds": [2],
  "remark": "修改后的备注"
}
```

说明：

- 路径参数 `id` 为唯一主键来源
- `username` 不作为修改入口字段
- 建议只传需要更新的字段

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.2.5 删除用户

**路径：** `DELETE /api/v1/users/{id}`\
**描述：** 删除单个用户，当前登录用户不可删除自己

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.2.6 批量删除用户

**路径：** `DELETE /api/v1/users/batch`\
**描述：** 批量删除用户，当前登录用户不可在删除列表中

**请求体：**

```json
{
  "ids": [1, 2, 3]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.2.7 修改密码

**路径：** `PUT /api/v1/users/{id}/password`\
**描述：** 修改指定用户密码

**请求体：**

```json
{
  "oldPassword": "old_password",
  "newPassword": "new_password"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.2.8 扩展接口一览

| 方法 | 路径 | 描述 | 说明 |
| --- | --- | --- | --- |
| `PUT` | `/api/v1/users/changeStatus` | 修改用户状态 | 请求体使用系统实体字段，至少传 `userId`、`status` |
| `GET` | `/api/v1/users/authRole/{userId}` | 获取用户授权信息 | 返回 `user` 与 `roles` |
| `PUT` | `/api/v1/users/authRole` | 保存用户授权角色 | 参数为 `userId`、`roleIds` |
| `GET` | `/api/v1/users/deptTree` | 获取用户可选组织树 | 返回组织树 |
| `POST` | `/api/v1/users/export` | 导出用户数据 | 文件流接口 |
| `POST` | `/api/v1/users/importData` | 导入用户数据 | `multipart/form-data` |
| `POST` | `/api/v1/users/importTemplate` | 下载导入模板 | 文件流接口 |

#### 2.2.9 常见失败响应

**未登录或Token无效：**

```json
{
  "code": 401,
  "msg": "请求访问：/api/v1/users/page，认证失败，无法访问系统资源",
  "timestamp": 1735689213000
}
```

**无接口权限：**

```json
{
  "code": 403,
  "msg": "没有权限，请联系管理员授权",
  "timestamp": 1735689213000
}
```

**参数校验失败：**

```json
{
  "code": 400,
  "msg": "用户名不能为空",
  "timestamp": 1735689213000
}
```

**业务失败示例：**

```json
{
  "code": 500,
  "msg": "用户不存在",
  "timestamp": 1735689213000
}
```

补充说明：

- 当前登录用户删除自己时会返回业务错误
- 用户名、手机号、邮箱唯一性冲突时会返回业务错误消息
- 请求参数类型不匹配时会返回 `400`

### 2.3 角色管理

说明：

- 角色状态：`0` 正常，`1` 停用
- 数据权限：`1` 全部数据，`2` 自定义数据，`3` 本组织数据，`4` 本组织及下级，`5` 仅本人数据
- 修改角色时以路径参数 `id` 为准，不依赖请求体中的 `id`

#### 2.3.0 权限要求

| 接口 | 权限标识 |
| --- | --- |
| `GET /api/v1/roles/page` | `system:role:list` |
| `GET /api/v1/roles/{id}` | `system:role:query` |
| `POST /api/v1/roles` | `system:role:add` |
| `PUT /api/v1/roles/{id}` | `system:role:edit` |
| `DELETE /api/v1/roles/{id}` | `system:role:remove` |
| `DELETE /api/v1/roles/batch` | `system:role:remove` |
| `PUT /api/v1/roles/{id}/dataScope` | `system:role:edit` |
| `PUT /api/v1/roles/{id}/status` | `system:role:edit` |

#### 2.3.1 分页查询角色

**路径：** `GET /api/v1/roles/page`\
**描述：** 分页查询角色列表

**请求参数：**

| 参数       | 类型     | 必填 | 说明   |
| -------- | ------ | -- | ---- |
| pageNum  | int    | 否  | 页码   |
| pageSize | int    | 否  | 每页数量 |
| code     | string | 否  | 角色编码 |
| name     | string | 否  | 角色名称 |
| status   | int    | 否  | 状态   |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 10,
    "rows": [
      {
        "id": 1,
        "code": "ADMIN",
        "name": "超级管理员",
        "description": "系统超级管理员",
        "dataScope": 1,
        "sortOrder": 0,
        "status": 0,
        "createTime": "2024-01-01 00:00:00",
        "createBy": "system",
        "updateTime": "2024-01-10 10:00:00",
        "updateBy": "admin"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 2.3.2 获取角色详情

**路径：** `GET /api/v1/roles/{id}`\
**描述：** 获取角色详情及菜单权限

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "ADMIN",
    "name": "超级管理员",
    "description": "系统超级管理员",
    "dataScope": 1,
    "sortOrder": 0,
    "status": 0,
    "menuIds": [1, 2, 3],
    "createTime": "2024-01-01 00:00:00",
    "createBy": "system",
    "updateTime": "2024-01-10 10:00:00",
    "updateBy": "admin"
  },
  "timestamp": 1735689213000
}
```

#### 2.3.3 新增角色

**路径：** `POST /api/v1/roles`\
**描述：** 新增角色

**请求体：**

```json
{
  "code": "TEST_ROLE",
  "name": "测试角色",
  "description": "测试角色描述",
  "dataScope": 1,
  "sortOrder": 10,
  "status": 0,
  "menuIds": [1, 2],
  "deptIds": [1, 2]
}
```

说明：

- `code`、`name`、`sortOrder` 为新增时必填
- `deptIds` 在 `dataScope = 2` 时通常需要传入

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 4
  },
  "timestamp": 1735689213000
}
```

#### 2.3.4 修改角色

**路径：** `PUT /api/v1/roles/{id}`\
**描述：** 修改角色

**请求体：**

```json
{
  "name": "测试角色修改",
  "description": "修改后的描述",
  "dataScope": 2,
  "sortOrder": 20,
  "status": 0,
  "menuIds": [1, 2, 3],
  "deptIds": [1, 2]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.3.5 删除角色

**路径：** `DELETE /api/v1/roles/{id}`\
**描述：** 删除单个角色，角色下存在用户时不可删除

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.3.6 批量删除角色

**路径：** `DELETE /api/v1/roles/batch`\
**描述：** 批量删除角色

**请求体：**

```json
{
  "ids": [2, 3, 4]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.3.7 保存数据权限

**路径：** `PUT /api/v1/roles/{id}/dataScope`\
**描述：** 保存角色数据权限

**请求体：**

```json
{
  "dataScope": "2",
  "deptIds": [1, 2]
}
```

说明：

- 接口底层仍使用系统实体字段，`dataScope` 实际按字符串值处理
- `deptIds` 仅在自定义数据权限时使用

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.3.8 状态修改

**路径：** `PUT /api/v1/roles/{id}/status`\
**描述：** 修改角色状态

**请求体：**

```json
{
  "status": "1"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.3.9 扩展接口一览

| 方法 | 路径 | 描述 | 说明 |
| --- | --- | --- | --- |
| `GET` | `/api/v1/roles/optionselect` | 获取角色下拉列表 | 返回原系统角色实体结构，如 `roleId / roleName / roleKey / status` |
| `GET` | `/api/v1/roles/{id}/deptTree` | 获取角色组织树及已选节点 | 返回 `checkedKeys` 与 `depts` |
| `POST` | `/api/v1/roles/export` | 导出角色数据 | 文件流接口 |
| `GET` | `/api/v1/roles/authUser/allocatedList` | 查询已分配用户列表 | 使用系统实体字段过滤 |
| `GET` | `/api/v1/roles/authUser/unallocatedList` | 查询未分配用户列表 | 使用系统实体字段过滤 |
| `PUT` | `/api/v1/roles/authUser/cancel` | 取消单个用户授权 | 请求体为 `userId / roleId` |
| `PUT` | `/api/v1/roles/authUser/cancelAll` | 批量取消用户授权 | 参数为 `roleId / userIds` |
| `PUT` | `/api/v1/roles/authUser/selectAll` | 批量选择用户授权 | 参数为 `roleId / userIds` |

#### 2.3.10 常见失败响应

**未登录或Token无效：**

```json
{
  "code": 401,
  "msg": "请求访问：/api/v1/roles/page，认证失败，无法访问系统资源",
  "timestamp": 1735689213000
}
```

**无接口权限：**

```json
{
  "code": 403,
  "msg": "没有权限，请联系管理员授权",
  "timestamp": 1735689213000
}
```

**业务失败示例：**

```json
{
  "code": 500,
  "msg": "角色不存在",
  "timestamp": 1735689213000
}
```

```json
{
  "code": 500,
  "msg": "新增角色'测试角色'失败，角色名称已存在",
  "timestamp": 1735689213000
}
```

补充说明：

- 角色下已分配用户时，删除会返回业务错误
- 超级管理员角色受保护，部分修改和删除场景会被拦截

### 2.4 菜单管理

说明：

- `code` 字段对应后端 `routeName`，表示菜单编码/路由名称
- `perms` 表示权限标识
- 菜单类型：`0` 目录，`1` 菜单，`2` 按钮
- 显示状态：`0` 显示，`1` 隐藏
- 菜单状态：`0` 正常，`1` 停用

#### 2.4.0 权限要求

| 接口 | 权限标识 |
| --- | --- |
| `GET /api/v1/menus/tree` | `system:menu:list` |
| `GET /api/v1/menus/current` | 当前登录用户菜单，无额外菜单管理权限要求 |
| `GET /api/v1/menus/{id}` | `system:menu:query` |
| `POST /api/v1/menus` | `system:menu:add` |
| `PUT /api/v1/menus/{id}` | `system:menu:edit` |
| `DELETE /api/v1/menus/{id}` | `system:menu:remove` |

#### 2.4.1 获取菜单列表（树形）

**路径：** `GET /api/v1/menus/tree`\
**描述：** 获取树形菜单结构

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "parentId": 0,
      "name": "系统管理",
      "code": "system",
      "path": "/system",
      "component": null,
      "icon": "setting",
      "type": 0,
      "visible": 0,
      "isCache": 1,
      "sortOrder": 0,
      "status": 0,
      "perms": null,
      "children": [
        {
          "id": 2,
          "parentId": 1,
          "name": "用户管理",
          "code": "user",
          "path": "/system/users",
          "component": "system/Users",
          "icon": "user",
          "type": 1,
          "visible": 0,
          "isCache": 0,
          "sortOrder": 0,
          "status": 0,
          "perms": "system:user:list",
          "children": []
        }
      ]
    }
  ],
  "timestamp": 1735689213000
}
```

#### 2.4.2 获取当前用户菜单

**路径：** `GET /api/v1/menus/current`\
**描述：** 获取当前登录用户有权限的菜单

**成功响应：** 同 `GET /api/v1/menus/tree`

#### 2.4.3 获取菜单详情

**路径：** `GET /api/v1/menus/{id}`\
**描述：** 获取菜单详情

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "parentId": 0,
    "name": "系统管理",
    "code": "system",
    "path": "/system",
    "component": null,
    "icon": "setting",
    "type": 0,
    "visible": 0,
    "perms": null,
    "isCache": 1,
    "sortOrder": 0,
    "status": 0,
    "createTime": "2024-01-01 00:00:00"
  },
  "timestamp": 1735689213000
}
```

#### 2.4.4 新增菜单

**路径：** `POST /api/v1/menus`\
**描述：** 新增菜单

**请求体：**

```json
{
  "parentId": 1,
  "name": "测试菜单",
  "code": "testMenu",
  "path": "/system/test",
  "component": "system/Test",
  "icon": "test",
  "type": 1,
  "visible": 0,
  "perms": "system:test:view",
  "isCache": 0,
  "sortOrder": 10,
  "status": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| parentId | long | 是 | 父级菜单ID |
| name | string | 是 | 菜单名称 |
| code | string | 否 | 菜单编码，对应 `routeName` |
| path | string | 否 | 路由地址，目录/菜单场景建议传 |
| component | string | 否 | 组件路径，菜单场景建议传 |
| icon | string | 否 | 图标 |
| type | int | 是 | `0目录 / 1菜单 / 2按钮` |
| visible | int | 否 | `0显示 / 1隐藏` |
| perms | string | 否 | 权限标识，按钮场景通常必传 |
| isCache | int | 否 | `0缓存 / 1不缓存` |
| sortOrder | int | 是 | 排序 |
| status | int | 否 | `0正常 / 1停用` |

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 100
  },
  "timestamp": 1735689213000
}
```

#### 2.4.5 修改菜单

**路径：** `PUT /api/v1/menus/{id}`\
**描述：** 修改菜单

**请求体：**

```json
{
  "parentId": 1,
  "name": "测试菜单修改",
  "code": "testMenu",
  "path": "/system/test/update",
  "component": "system/TestUpdate",
  "icon": "test-update",
  "type": 1,
  "visible": 0,
  "perms": "system:test:view",
  "isCache": 0,
  "sortOrder": 20,
  "status": 0
}
```

说明：

- 路径参数 `id` 为唯一主键来源
- 上级菜单不能选择自己

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.4.6 删除菜单

**路径：** `DELETE /api/v1/menus/{id}`\
**描述：** 删除菜单，存在子菜单或已分配角色时不可删除

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.4.7 扩展接口一览

| 方法 | 路径 | 描述 | 说明 |
| --- | --- | --- | --- |
| `GET` | `/api/v1/menus/treeselect` | 获取菜单下拉树 | 返回 `TreeSelect` 结构：`id / label / children` |
| `GET` | `/api/v1/menus/roleMenuTreeselect/{roleId}` | 获取角色菜单树 | 返回 `checkedKeys` 与 `menus` |
| `PUT` | `/api/v1/menus/updateSort` | 批量保存菜单排序 | 请求体包含 `menuIds`、`orderNums`，逗号分隔 |
| `DELETE` | `/api/v1/menus/batch` | 批量删除菜单 | 请求体为 `Long[]` |

#### 2.4.8 常见失败响应

**未登录或Token无效：**

```json
{
  "code": 401,
  "msg": "请求访问：/api/v1/menus/tree，认证失败，无法访问系统资源",
  "timestamp": 1735689213000
}
```

**无接口权限：**

```json
{
  "code": 403,
  "msg": "没有权限，请联系管理员授权",
  "timestamp": 1735689213000
}
```

**业务失败示例：**

```json
{
  "code": 500,
  "msg": "菜单不存在",
  "timestamp": 1735689213000
}
```

```json
{
  "code": 500,
  "msg": "新增菜单'测试菜单'失败，菜单名称已存在",
  "timestamp": 1735689213000
}
```

补充说明：

- 上级菜单选择自己时会返回业务错误
- 存在子菜单或菜单已分配给角色时，删除会返回业务错误

### 2.5 组织管理

说明：

- 组织状态：`0` 正常，`1` 停用
- 当前组织管理基于 `sys_dept` 扩展字段实现
- 组织详情中 `parentIds` 采用 `/0/1/` 格式

#### 2.5.0 权限要求

说明：组织接口路径为 `/api/v1/organizations/*`，但权限标识沿用部门模块的 `system:dept:*`。

| 接口 | 权限标识 |
| --- | --- |
| `GET /api/v1/organizations/tree` | `system:dept:list` |
| `GET /api/v1/organizations/page` | `system:dept:list` |
| `GET /api/v1/organizations/{id}` | `system:dept:query` |
| `POST /api/v1/organizations` | `system:dept:add` |
| `PUT /api/v1/organizations/{id}` | `system:dept:edit` |
| `DELETE /api/v1/organizations/{id}` | `system:dept:remove` |

#### 2.5.1 获取组织列表（树形）

**路径：** `GET /api/v1/organizations/tree`\
**描述：** 获取树形组织结构

**请求参数：**

| 参数   | 类型     | 必填 | 说明   |
| ---- | ------ | -- | ---- |
| code | string | 否  | 组织编码 |
| name | string | 否  | 组织名称 |
| status | int | 否 | 状态 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "code": "ROOT",
      "name": "系统管理员",
      "parentId": 0,
      "parentIds": "/0/",
      "level": 1,
      "leader": "admin",
      "phone": "13800138000",
      "email": "admin@example.com",
      "region": "成都",
      "address": "成华区龙潭寺",
      "status": 0,
      "sortOrder": 0,
      "children": [
        {
          "id": 2,
          "code": "DEPT001",
          "name": "监测中心",
          "parentId": 1,
          "parentIds": "/0/1/",
          "level": 2,
          "status": 0,
          "sortOrder": 0,
          "children": []
        }
      ]
    }
  ],
  "timestamp": 1735689213000
}
```

#### 2.5.2 分页查询组织

**路径：** `GET /api/v1/organizations/page`\
**描述：** 分页查询组织

**请求参数：**

| 参数       | 类型     | 必填 | 说明   |
| -------- | ------ | -- | ---- |
| pageNum  | int    | 否  | 页码   |
| pageSize | int    | 否  | 每页数量 |
| code     | string | 否  | 组织编码 |
| name     | string | 否  | 组织名称 |
| status   | int    | 否  | 状态   |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 10,
    "rows": [
      {
        "id": 1,
        "code": "ROOT",
        "name": "系统管理员",
        "parentId": 0,
        "parentIds": "/0/",
        "level": 1,
        "leader": "admin",
        "phone": "13800138000",
        "email": "admin@example.com",
        "region": "成都",
        "address": "成华区龙潭寺",
        "status": 0,
        "sortOrder": 0,
        "createTime": "2024-01-01 00:00:00",
        "updateTime": "2024-01-10 10:00:00"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 2.5.3 获取组织详情

**路径：** `GET /api/v1/organizations/{id}`\
**描述：** 获取组织详情

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "ROOT",
    "name": "系统管理员",
    "parentId": 0,
    "parentIds": "/0/",
    "level": 1,
    "leader": "admin",
    "phone": "13800138000",
    "email": "admin@example.com",
    "region": "成都",
    "address": "成华区龙潭寺",
    "status": 0,
    "sortOrder": 0,
    "createTime": "2024-01-01 00:00:00",
    "updateTime": "2024-01-10 10:00:00"
  },
  "timestamp": 1735689213000
}
```

#### 2.5.4 新增组织

**路径：** `POST /api/v1/organizations`\
**描述：** 新增组织

**请求体：**

```json
{
  "code": "DEPT003",
  "name": "测试部门",
  "parentId": 1,
  "leader": "张三",
  "phone": "13800138001",
  "email": "zhangsan@example.com",
  "region": "成都",
  "address": "测试地址",
  "sortOrder": 10,
  "status": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | 是 | 组织编码 |
| name | string | 是 | 组织名称 |
| parentId | long | 是 | 父组织ID，根节点传 `0` |
| leader | string | 否 | 负责人 |
| phone | string | 否 | 联系电话 |
| email | string | 否 | 邮箱 |
| region | string | 否 | 区域 |
| address | string | 否 | 地址 |
| sortOrder | int | 是 | 排序 |
| status | int | 否 | 状态，建议传 `0` |

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 4
  },
  "timestamp": 1735689213000
}
```

#### 2.5.5 修改组织

**路径：** `PUT /api/v1/organizations/{id}`\
**描述：** 修改组织

**请求体：**

```json
{
  "code": "DEPT003",
  "name": "测试部门修改",
  "parentId": 1,
  "leader": "李四",
  "phone": "13800138002",
  "email": "lisi@example.com",
  "region": "成都",
  "address": "修改后的地址",
  "sortOrder": 20,
  "status": 0
}
```

说明：

- 路径参数 `id` 为唯一主键来源
- `parentId` 为必填字段
- 当组织存在未停用子组织时，不能直接停用当前组织

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.5.6 删除组织

**路径：** `DELETE /api/v1/organizations/{id}`\
**描述：** 删除组织，存在下级组织或组织下存在用户时不可删除

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 2.5.7 常见失败响应

**未登录或Token无效：**

```json
{
  "code": 401,
  "msg": "请求访问：/api/v1/organizations/page，认证失败，无法访问系统资源",
  "timestamp": 1735689213000
}
```

**无接口权限：**

```json
{
  "code": 403,
  "msg": "没有权限，请联系管理员授权",
  "timestamp": 1735689213000
}
```

**参数校验失败：**

```json
{
  "code": 400,
  "msg": "父组织不能为空",
  "timestamp": 1735689213000
}
```

**业务失败示例：**

```json
{
  "code": 500,
  "msg": "该组织包含未停用的子组织！",
  "timestamp": 1735689213000
}
```

补充说明：

- 上级组织停用时，新增或修改下级组织会返回业务错误
- 存在下级组织或组织下存在用户时，删除会返回业务错误

### 2.6 系统日志模块

#### 2.6.1 分页查询操作日志

**路径：** `GET /api/v1/logs/operations/page`\
**描述：** 按条件分页查询接口调用操作日志

**请求头：**

| 参数            | 类型     | 必填 | 说明           |
| ------------- | ------ | -- | ------------ |
| Authorization | string | 是  | Bearer Token |

**请求参数：**

| 参数         | 类型     | 必填 | 说明                            |
| ---------- | ------ | -- | ----------------------------- |
| pageNum    | int    | 否  | 页码，默认 1                       |
| pageSize   | int    | 否  | 每页数量，默认 10                    |
| username   | string | 否  | 操作用户名                         |
| title      | string | 否  | 业务标题关键字                       |
| apiPath    | string | 否  | 接口路径关键字                       |
| execStatus | string | 否  | 执行状态，`SUCCESS` / `FAIL`       |
| startTime  | string | 否  | 开始时间，格式 `yyyy-MM-dd HH:mm:ss` |
| endTime    | string | 否  | 结束时间，格式 `yyyy-MM-dd HH:mm:ss` |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 1,
    "rows": [
      {
        "id": 11,
        "eventId": 921779534004781100,
        "logType": "OPERATION",
        "title": "用户管理",
        "businessType": "INSERT",
        "apiPath": "/api/v1/system/users",
        "requestMethod": "POST",
        "controllerMethod": "com.zwei.web.controller.system.SysUserController.add",
        "username": "admin",
        "userId": 1,
        "clientIp": "127.0.0.1",
        "httpStatus": 200,
        "execStatus": "SUCCESS",
        "costTimeMs": 32,
        "occurredAt": "2026-05-23 18:30:15.123"
      }
    ]
  },
  "timestamp": 1779534175000
}
```

#### 2.6.2 分页查询认证日志

**路径：** `GET /api/v1/logs/auth/page`\
**描述：** 分页查询登录、登出、未认证、认证失败等身份认证日志

**请求头：**

| 参数            | 类型     | 必填 | 说明           |
| ------------- | ------ | -- | ------------ |
| Authorization | string | 是  | Bearer Token |

**请求参数：**

| 参数            | 类型     | 必填 | 说明                                                            |
| ------------- | ------ | -- | ------------------------------------------------------------- |
| pageNum       | int    | 否  | 页码，默认 1                                                       |
| pageSize      | int    | 否  | 每页数量，默认 10                                                    |
| username      | string | 否  | 用户名                                                           |
| authEventType | string | 否  | 认证事件类型，如 `LOGIN_SUCCESS`、`LOGIN_FAIL`、`LOGOUT`、`UNAUTHORIZED` |
| resultStatus  | string | 否  | 结果状态，`SUCCESS` / `FAIL`                                       |
| startTime     | string | 否  | 开始时间，格式 `yyyy-MM-dd HH:mm:ss`                                 |
| endTime       | string | 否  | 结束时间，格式 `yyyy-MM-dd HH:mm:ss`                                 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 2,
    "rows": [
      {
        "id": 74,
        "eventId": 921779534004781002,
        "logType": "AUTH",
        "username": "admin",
        "authEventType": "LOGIN_SUCCESS",
        "authChannel": "PASSWORD",
        "requestUri": "/api/v1/auth/login",
        "requestMethod": "POST",
        "clientIp": "127.0.0.1",
        "resultStatus": "SUCCESS",
        "failureMessage": "登录成功",
        "occurredAt": "2026-05-23 18:47:03.083"
      }
    ]
  },
  "timestamp": 1779533272847
}
```

#### 2.6.3 分页查询运行日志

**路径：** `GET /api/v1/logs/runtime/page`\
**描述：** 分页查询系统运行期控制台日志

**请求头：**

| 参数            | 类型     | 必填 | 说明           |
| ------------- | ------ | -- | ------------ |
| Authorization | string | 是  | Bearer Token |

**请求参数：**

| 参数         | 类型     | 必填 | 说明                            |
| ---------- | ------ | -- | ----------------------------- |
| pageNum    | int    | 否  | 页码，默认 1                       |
| pageSize   | int    | 否  | 每页数量，默认 10                    |
| level      | string | 否  | 日志级别，如 `INFO`、`WARN`、`ERROR`  |
| loggerName | string | 否  | Logger 名称关键字                  |
| keyword    | string | 否  | 日志内容关键字                       |
| startTime  | string | 否  | 开始时间，格式 `yyyy-MM-dd HH:mm:ss` |
| endTime    | string | 否  | 结束时间，格式 `yyyy-MM-dd HH:mm:ss` |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "pageNum": 1,
    "pageSize": 5,
    "total": 25,
    "rows": [
      {
        "id": 25,
        "eventId": 921779534004781012,
        "logType": "RUNTIME",
        "level": "WARN",
        "loggerName": "net.dreamlu.mica.net.server.task.ServerHeartbeatTask",
        "threadName": "DefaultTimerTaskService",
        "message": "Mica-Mqtt-Server, 检查心跳, 共0个连接...",
        "messageDigest": "Mica-Mqtt-Server, 检查心跳, 共0个连接...",
        "sourceApp": "zwei-admin",
        "occurredAt": "2026-05-23 19:02:11.146"
      }
    ]
  },
  "timestamp": 1779533268655
}
```

#### 2.6.4 SSE 实时订阅日志流

**路径：** `GET /api/v1/logs/stream`\
**描述：** 通过 SSE 订阅实时日志流，支持按日志类型筛选，并支持 `Last-Event-ID` 与 checkpoint 断线恢复

**请求头：**

| 参数            | 类型     | 必填 | 说明                       |
| ------------- | ------ | -- | ------------------------ |
| Authorization | string | 是  | Bearer Token             |
| Accept        | string | 是  | 固定为 `text/event-stream`  |
| Last-Event-ID | string | 否  | 上次收到的事件 ID，断线重连时用于补发历史日志 |

**请求参数：**

| 参数            | 类型     | 必填 | 说明                                           |
| ------------- | ------ | -- | -------------------------------------------- |
| types         | string | 否  | 订阅日志类型，多个用逗号分隔，支持 `operation,auth,runtime`   |
| subscriberKey | string | 否  | 订阅者唯一标识，用于 checkpoint 持久化恢复；不传则服务端按用户和类型自动生成 |

**SSE 事件说明：**

| 事件名       | 说明                                   |
| --------- | ------------------------------------ |
| ready     | 连接建立成功，`data.resumeEventId` 表示本次恢复起点 |
| operation | 实时操作日志事件                             |
| auth      | 实时认证日志事件                             |
| runtime   | 实时运行日志事件                             |
| replay    | 断线恢复补发的历史日志事件                        |

**SSE 返回示例：**

```text
event:ready
retry:3000
data:{"resumeEventId":921779534004781002}

id:921779534004781003
event:replay
retry:3000
data:{"logType":"AUTH","authEventType":"UNAUTHORIZED","requestUri":"/api/v1/logs/runtime/page","resultStatus":"FAIL"}

id:921779534004781012
event:runtime
retry:3000
data:{"logType":"RUNTIME","level":"WARN","loggerName":"net.dreamlu.mica.net.server.task.ServerHeartbeatTask"}
```

#### 2.6.5 SSE 前端接入说明

**适用场景：** 日志大屏、管理后台日志监控页、告警联动组件

**连接规则：**

1. 前端使用浏览器原生 `EventSource` 或支持自定义 Header 的 SSE 客户端。
2. 浏览器原生 `EventSource` 无法直接追加 `Authorization` 请求头，建议通过同源鉴权、反向代理注入 Header，或使用支持 Header 的 SSE 封装库。
3. 建议为同一终端传入稳定的 `subscriberKey`，例如 `log-screen-admin`，以便服务端持久化 checkpoint。
4. 断线重连时，优先带上 `Last-Event-ID`；若未携带，服务端将尝试按 `subscriberKey` 读取 checkpoint 继续补发。

**前端示例（使用 fetch-event-source）：**

```ts
import { fetchEventSource } from '@microsoft/fetch-event-source'

const token = localStorage.getItem('token')
let lastEventId = ''

fetchEventSource('/api/v1/logs/stream?types=auth,runtime&subscriberKey=log-screen-admin', {
  headers: {
    Authorization: `Bearer ${token}`,
    Accept: 'text/event-stream',
    ...(lastEventId ? { 'Last-Event-ID': lastEventId } : {})
  },
  onopen(response) {
    if (!response.ok) {
      throw new Error(`SSE连接失败: ${response.status}`)
    }
  },
  onmessage(event) {
    if (event.id) {
      lastEventId = event.id
    }
    const payload = JSON.parse(event.data)
    if (event.event === 'ready') {
      console.log('SSE连接成功', payload.resumeEventId)
      return
    }
    if (event.event === 'replay') {
      console.log('补发日志', payload)
      return
    }
    console.log('实时日志', event.event, payload)
  },
  onerror(error) {
    console.error('SSE连接异常', error)
    throw error
  }
})
```

#### 2.6.6 获取当前 SSE 活跃连接数

**路径：** `GET /api/v1/logs/stream/connections`\
**描述：** 查询当前日志流 SSE 活跃连接数

**请求头：**

| 参数            | 类型     | 必填 | 说明           |
| ------------- | ------ | -- | ------------ |
| Authorization | string | 是  | Bearer Token |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "activeConnections": 1
  },
  "timestamp": 1779534175000
}
```

#### 2.6.7 获取系统监控总览

**路径：** `GET /api/v1/monitor/overview`\
**描述：** 聚合服务器健康、Redis 状态、在线用户数、MQTT 核心指标和系统运行时长

**权限标识：** `monitor:overview:list`

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "server": { "cpu": {...}, "mem": {...}, "jvm": {...}, "sys": {...}, "sysFiles": [...] },
    "redis": {
      "onlineUserCount": 3,
      "totalKeys": 2456
    },
    "uptime": {
      "startTime": 1749000000000,
      "uptimeMs": 86400000,
      "uptimeFormatted": "1天 0小时 0分"
    },
    "mqtt": {
      "upstream": true,
      "connections": {
        "accepted": 156,
        "closed": 144,
        "size": 12
      },
      "messages": {
        "bytesPerTcpReceive": 27.0,
        "handledBytes": 45678901,
        "handledPackets": 890123,
        "packetsPerTcpReceive": 1.0,
        "receivedBytes": 23000000,
        "receivedPackets": 450000,
        "sendBytes": 22000000,
        "sendPackets": 440000
      },
      "nodes": {
        "clientNodes": 12,
        "connections": 12,
        "users": 12
      },
      "startTime": 1749000012000,
      "connectionsSize": 12,
      "connectionsAccepted": 156,
      "connectionsClosed": 144,
      "messagesHandledPackets": 890123,
      "messagesHandledBytes": 45678901,
      "messagesReceivedPackets": 450000,
      "messagesReceivedBytes": 23000000,
      "messagesSendPackets": 440000,
      "messagesSendBytes": 22000000
    }
  },
  "timestamp": 1735689213000
}
```

---

### 2.7 系统监控模块接口

> **模块说明：** 以下接口由 `zwei-monitor` 模块统一对外暴露，整合系统级监控能力并新增 MQTT 服务器实时状态查询。旧路径
`/sys/v1/monitor/*`（服务器监控、缓存监控、在线用户）仍可用，但计划在未来版本移除，请新开发直接使用本章节接口。

#### 2.7.1 MQTT 服务器统计

**路径：** `GET /api/v1/monitor/mqtt/stats`\
**描述：** 获取 MQTT 服务器全量运行统计（通过内调 mica-mqtt HTTP API 获取实时数据）

**权限标识：** `monitor:mqtt:list`

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "upstream": true,
    "connections": {
      "accepted": 156,
      "closed": 144,
      "size": 12
    },
    "messages": {
      "bytesPerTcpReceive": 27.0,
      "handledBytes": 45678901,
      "handledPackets": 890123,
      "packetsPerTcpReceive": 1.0,
      "receivedBytes": 23000000,
      "receivedPackets": 450000,
      "sendBytes": 22000000,
      "sendPackets": 440000
    },
    "nodes": {
      "clientNodes": 12,
      "connections": 12,
      "users": 12
    },
    "startTime": 1749000012000,
    "connectionsSize": 12,
    "connectionsAccepted": 156,
    "connectionsClosed": 144,
    "messagesHandledPackets": 890123,
    "messagesHandledBytes": 45678901,
    "messagesReceivedPackets": 450000,
    "messagesReceivedBytes": 23000000,
    "messagesSendPackets": 440000,
    "messagesSendBytes": 22000000
  },
  "timestamp": 1735689213000
}
```

**字段说明：**

| 字段                              | 类型      | 说明                          |
|---------------------------------|---------|-----------------------------|
| upstream                        | boolean | mica-mqtt API 是否可达          |
| `connections.accepted`          | long    | 历史累计接受连接数                   |
| `connections.closed`            | long    | 历史累计关闭连接数                   |
| `connections.size`              | long    | 当前活跃连接数                     |
| `messages.bytesPerTcpReceive`   | double  | TCP 接收字节速率                  |
| `messages.handledBytes`         | long    | 已处理消息字节总数                   |
| `messages.handledPackets`       | long    | 已处理消息包总数                    |
| `messages.packetsPerTcpReceive` | double  | TCP 接收包速率                   |
| `messages.receivedBytes`        | long    | 已接收消息字节总数                   |
| `messages.receivedPackets`      | long    | 已接收消息包总数                    |
| `messages.sendBytes`            | long    | 已发送消息字节总数                   |
| `messages.sendPackets`          | long    | 已发送消息包总数                    |
| `nodes.clientNodes`             | int     | 客户端节点数                      |
| `nodes.connections`             | int     | 连接数（与 connections.size 一致）  |
| `nodes.users`                   | int     | 用户数                         |
| startTime                       | Long    | MQTT 服务启动时间戳（毫秒），不可用时为 null |

> 同时提供扁平化的便捷字段（`connectionsSize`、`messagesHandledPackets` 等），前端可按需使用嵌套或扁平格式。

---

#### 2.7.2 MQTT 监听器列表

**路径：** `GET /api/v1/monitor/mqtt/listeners`\
**描述：** 获取所有 MQTT 监听器配置（TCP/WS/SSL/HTTP）

**权限标识：** `monitor:mqtt:list`

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": [
    {
      "type": "mqtt-tcp",
      "ip": "0.0.0.0",
      "port": 1883,
      "enabled": true,
      "remark": "设备 MQTT 直连"
    },
    {
      "type": "mqtt-ws",
      "ip": "0.0.0.0",
      "port": 8083,
      "enabled": true,
      "remark": "浏览器 WebSocket 接入"
    },
    {
      "type": "mqtt-ssl",
      "ip": "0.0.0.0",
      "port": 8883,
      "enabled": false,
      "remark": "SSL/TLS 加密接入"
    },
    {
      "type": "mqtt-http",
      "ip": "127.0.0.1",
      "port": 18083,
      "enabled": true,
      "remark": "HTTP 管理 API (内部)"
    }
  ],
  "timestamp": 1735689213000
}
```

---

#### 2.7.3 MQTT 运行配置

**路径：** `GET /api/v1/monitor/mqtt/config`\
**描述：** 获取 MQTT 服务器运行配置参数摘要

**权限标识：** `monitor:mqtt:list`

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "heartbeatTimeout": 120000,
    "readBufferSize": "8KB",
    "maxBytesInMessage": "10MB",
    "authEnabled": true,
    "debug": false,
    "statEnable": true
  },
  "timestamp": 1735689213000
}
```

---

#### 2.7.4 分页查询连接客户端

**路径：** `GET /api/v1/monitor/mqtt/clients/page`\
**描述：** 分页查询当前连接的 MQTT 客户端列表（已富化设备名称、隐患点名称等业务信息）

**权限标识：** `monitor:mqtt:list`

**请求参数：**

| 参数    | 类型  | 必填 | 说明         |
|-------|-----|----|------------|
| page  | int | 否  | 页码，默认 1    |
| limit | int | 否  | 每页大小，默认 20 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "pageNumber": 1,
    "pageSize": 20,
    "totalRow": 1,
    "list": [
      {
        "clientId": "mqttx_5fe4cfcf",
        "username": "A1B2C3",
        "connected": true,
        "ipAddress": "192.168.1.100",
        "port": 52341,
        "protoName": "MQTT",
        "protoVer": 5,
        "createdAt": 1749000012000,
        "connectedAt": 1749000012000,
        "deviceId": 1,
        "deviceName": "裂缝计-01",
        "deviceCode": "DEVICE001",
        "deviceRunStatus": 1,
        "hazardPointName": "隐患点A",
        "lastAuthIp": "192.168.1.100",
        "lastAuthTime": "2026-06-03 10:00:00"
      }
    ]
  },
  "timestamp": 1735689213000
}
```

---

#### 2.7.5 获取客户端详情（含订阅）

**路径：** `GET /api/v1/monitor/mqtt/clients/{clientId}`\
**描述：** 获取指定客户端的连接详情与主题订阅列表

**权限标识：** `monitor:mqtt:list`

**路径参数：**

| 参数       | 类型     | 必填 | 说明            |
|----------|--------|----|---------------|
| clientId | string | 是  | MQTT ClientID |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "info": {
      "clientId": "mqttx_5fe4cfcf",
      "username": "A1B2C3",
      "connected": true,
      "ipAddress": "192.168.1.100",
      "port": 52341,
      "protoName": "MQTT",
      "protoVer": 5,
      "createdAt": 1749000012000,
      "connectedAt": 1749000012000,
      "deviceId": 1,
      "deviceName": "裂缝计-01",
      "deviceCode": "DEVICE001",
      "deviceRunStatus": 1,
      "hazardPointName": "隐患点A",
      "lastAuthIp": "192.168.1.100",
      "lastAuthTime": "2026-06-03 10:00:00"
    },
    "subscriptions": [
      { "clientId": "mqttx_5fe4cfcf", "topicFilter": "#", "mqttQoS": 0 },
      { "clientId": "mqttx_5fe4cfcf", "topicFilter": "sys/v1/DEVICE001/1/updata", "mqttQoS": 1 }
    ]
  },
  "timestamp": 1735689213000
}
```

---

#### 2.7.6 踢出指定客户端

**路径：** `DELETE /api/v1/monitor/mqtt/clients/{clientId}`\
**描述：** 踢出指定 MQTT 客户端（断开连接并清除会话）。注意：客户端可能自动重连，如需永久踢出需配合黑名单机制。

**权限标识：** `monitor:mqtt:kick`

**路径参数：**

| 参数       | 类型     | 必填 | 说明            |
|----------|--------|----|---------------|
| clientId | string | 是  | MQTT ClientID |

**成功响应：**

```json
{ "code": 200, "msg": "已踢出客户端: mqttx_5fe4cfcf", "data": null, "timestamp": 1735689213000 }
```

**失败响应：**

```json
{ "code": 500, "msg": "踢出失败，请确认 MQTT HTTP API 已启用且客户端在线", "data": null, "timestamp": 1735689213000 }
```

---

#### 2.7.7 批量踢出客户端

**路径：** `DELETE /api/v1/monitor/mqtt/clients/batch`\
**描述：** 批量踢出多个 MQTT 客户端

**权限标识：** `monitor:mqtt:kick`

**请求体：**

```json
["mqttx_5fe4cfcf", "mqttx_abc123"]
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { "success": 1, "fail": 1, "total": 2 },
  "timestamp": 1735689213000
}
```

#### 2.7.8 MQTT 数据日志查询

**路径：** `GET /api/v1/monitor/mqtt/messages/page`\
**描述：** 分页查询最近通过平台转发的设备监测消息日志（按接收时间倒序），支持按 clientId、topic 过滤

**权限标识：** `monitor:mqtt:list`

**请求参数：**

| 参数       | 类型     | 必填 | 说明                     |
|----------|--------|----|------------------------|
| page     | int    | 否  | 页码，默认 1                |
| pageSize | int    | 否  | 每页大小，默认 20             |
| clientId | string | 否  | 按 clientId 模糊过滤，不传则不过滤 |
| topic    | string | 否  | 按 topic 模糊过滤，不传则不过滤    |

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "pageNumber": 1,
    "pageSize": 20,
    "totalRow": 150,
    "list": [
      {
        "receiveTime": 1749000123456,
        "clientId": "mqttx_5fe4cfcf",
        "username": "A1B2C3",
        "topic": "sys/v1/DEVICE001/1/updata",
        "payload": "{\"rainfall_hour\":12.5,\"water_level\":3.2}",
        "payloadSize": 42
      }
    ]
  },
  "timestamp": 1735689213000
}
```

**字段说明：**

| 字段          | 类型     | 说明                           |
|-------------|--------|------------------------------|
| receiveTime | long   | 消息接收时间戳（毫秒）                  |
| clientId    | string | MQTT Client ID               |
| username    | string | 设备认证用户名                      |
| topic       | string | 发布主题                         |
| payload     | string | 消息负载内容（截断至 500 字符，超出加 "..."） |
| payloadSize | int    | 原始报文大小（Byte）                 |

> **实现说明：** 日志存储在内存环形缓冲区中，最多保留最近 1000 条记录。服务重启后清空，不作为持久化审计数据源。

---

> **旧接口迁移说明：**
>
> 以下旧路径监控接口将在未来版本中移除，请尽快迁移到新接口：
>
> | 旧路径 | 替代方案 |
> |-------|---------|
> | `GET /sys/v1/monitor/server` | `GET /api/v1/monitor/overview` → `data.server` |
> | `GET /sys/v1/monitor/cache` | `GET /api/v1/monitor/overview` → `data.redis` |
> | `GET /sys/v1/monitor/online/list` | `GET /api/v1/monitor/overview` → `data.redis.onlineUserCount` |
> | `DELETE /sys/v1/monitor/online/{tokenId}` | 暂未迁移，后续版本补充 |

***

## 3. 基础管理模块接口

### 3.1 隐患点管理

#### 3.1.1 分页查询隐患点

**路径：** `GET /api/v1/hazard-points/page`\
**描述：** 分页查询隐患点列表

**说明：** `groupName` 为根据 `groupId` 联表查询返回的分组名称，数据库不再冗余存储该字段。

**请求参数：**

| 参数       | 类型     | 必填 | 说明    |
| -------- | ------ | -- | ----- |
| pageNum  | int    | 否  | 页码    |
| pageSize | int    | 否  | 每页数量  |
| code     | string | 否  | 隐患点编号 |
| name     | string | 否  | 隐患点名称 |
| groupId  | long   | 否  | 分组ID  |
| status   | int    | 否  | 状态    |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "rows": [
      {
        "id": 1,
        "code": "HP001",
        "name": "隐患点A",
        "groupId": 1,
        "groupName": "第一分组",
        "longitude": 104.156789,
        "latitude": 30.678901,
        "strike": 45.00,
        "description": "隐患描述",
        "status": 1,
        "statusName": "监测中",
        "deviceCount": 2,
        "createTime": "2024-01-10 10:00:00",
        "createBy": "admin"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 3.1.2 获取隐患点详情

**路径：** `GET /api/v1/hazard-points/{id}`\
**描述：** 获取隐患点详情

**说明：** `groupName` 为根据 `groupId` 联表查询返回的分组名称，数据库不再冗余存储该字段。

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "HP001",
    "name": "隐患点A",
    "groupId": 1,
    "groupName": "第一分组",
    "longitude": 104.156789,
    "latitude": 30.678901,
    "strike": 45.00,
    "description": "隐患描述",
    "status": 1,
    "statusName": "监测中",
    "deviceCount": 2,
    "createTime": "2024-01-10 10:00:00",
    "createBy": "admin",
    "updateTime": "2024-01-15 14:00:00",
    "updateBy": "admin"
  },
  "timestamp": 1735689213000
}
```

#### 3.1.3 新增隐患点

**路径：** `POST /api/v1/hazard-points`\
**描述：** 新增隐患点

**请求体：**

```json
{
  "code": "HP001",
  "name": "隐患点A",
  "groupId": 1,
  "longitude": 104.156789,
  "latitude": 30.678901,
  "strike": 45.00,
  "description": "隐患描述"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 3.1.4 修改隐患点

**路径：** `PUT /api/v1/hazard-points/{id}`\
**描述：** 修改隐患点

**请求体：**

```json
{
  "name": "隐患点A修改",
  "groupId": 2,
  "longitude": 104.156790,
  "latitude": 30.678902,
  "strike": 50.00,
  "description": "修改后的描述"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.1.5 删除隐患点

**路径：** `DELETE /api/v1/hazard-points/{id}`\
**描述：** 删除隐患点

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.1.6 批量删除隐患点

**路径：** `DELETE /api/v1/hazard-points/batch`\
**描述：** 批量删除隐患点

**请求体：**

```json
{
  "ids": [1, 2, 3]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "批量删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.1.7 停测/恢复隐患点

**路径：** `PUT /api/v1/hazard-points/{id}/pause`\
**描述：** 停测或恢复隐患点监测

**请求体：**

```json
{
  "pause": true
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.1.8 完结隐患点

**路径：** `PUT /api/v1/hazard-points/{id}/complete`\
**描述：** 完结隐患点

**成功响应：**

```json
{
  "code": 200,
  "msg": "完结成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.1.9 批量操作

**路径：** `PUT /api/v1/hazard-points/batch/operate`\
**描述：** 批量停测/恢复/完结隐患点

**请求体：**

```json
{
  "ids": [1, 2, 3],
  "operation": "pause"
}
```

| operation | 说明 |
| --------- | -- |
| pause     | 停测 |
| resume    | 恢复 |
| complete  | 完结 |

### 3.2 隐患点分组管理

#### 3.2.1 获取分组列表

**路径：** `GET /api/v1/hazard-point-groups`\
**描述：** 获取隐患点分组列表

**请求参数：**

| 参数     | 类型     | 必填 | 说明           |
| ------ | ------ | -- | ------------ |
| code   | string | 否  | 分组编码，支持模糊查询  |
| name   | string | 否  | 分组名称，支持模糊查询  |
| status | int    | 否  | 状态：0-禁用，1-启用 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "code": "GROUP001",
      "name": "第一分组",
      "description": "测试分组",
      "sortOrder": 0,
      "status": 1,
      "count": 10
    }
  ],
  "timestamp": 1735689213000
}
```

#### 3.2.2 新增分组

**路径：** `POST /api/v1/hazard-point-groups`\
**描述：** 新增分组

**请求体：**

```json
{
  "code": "GROUP001",
  "name": "第一分组",
  "description": "测试分组",
  "sortOrder": 0,
  "status": 1
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 3.2.3 修改分组

**路径：** `PUT /api/v1/hazard-point-groups/{id}`\
**描述：** 修改分组

**说明：** 支持部分字段更新；未传字段保持原值不变。

**请求体：**

```json
{
  "name": "第一分组修改",
  "description": "修改描述",
  "sortOrder": 10,
  "status": 1
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.2.4 删除分组

**路径：** `DELETE /api/v1/hazard-point-groups/{id}`\
**描述：** 删除分组

**说明：** 当分组下仍存在隐患点时不允许删除。

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

**失败响应示例：**

分组不存在：

```json
{
  "code": 404,
  "msg": "分组不存在",
  "timestamp": 1735689213000
}
```

分组下仍存在隐患点：

```json
{
  "code": 400,
  "msg": "该分组下存在隐患点，不允许删除",
  "timestamp": 1735689213000
}
```

#### 3.2.5 获取分组详情

**路径：** `GET /api/v1/hazard-point-groups/{id}`\
**描述：** 获取隐患点分组详情

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "GROUP001",
    "name": "第一分组",
    "description": "测试分组",
    "sortOrder": 0,
    "status": 1
  },
  "timestamp": 1735689213000
}
```

### 3.3 监测类型管理

#### 3.3.1 分页查询监测类型

**路径：** `GET /api/v1/monitor-types/page`\
**描述：** 分页查询监测类型

**请求参数：**

| 参数         | 类型     | 必填 | 说明   |
| ---------- | ------ | -- | ---- |
| pageNum    | int    | 否  | 页码   |
| pageSize   | int    | 否  | 每页数量 |
| code       | string | 否  | 编码   |
| name       | string | 否  | 名称   |
| deviceType | int    | 否  | 设备类型 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 10,
    "rows": [
      {
        "id": 1,
        "code": "JCLX001",
        "name": "雨量监测",
        "deviceType": 2,
        "deviceTypeName": "传感器",
        "icon": "/jc-icon/green/yl_green.png",
        "description": "雨量监测类型",
        "sortOrder": 0,
        "status": 1,
        "createTime": "2024-01-01 00:00:00"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 3.3.2 获取监测类型列表

**路径：** `GET /api/v1/monitor-types`\
**描述：** 获取所有监测类型列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "code": "JCLX001",
      "name": "雨量监测",
      "deviceType": 2,
      "icon": "/jc-icon/green/yl_green.png"
    }
  ],
  "timestamp": 1735689213000
}
```

#### 3.3.3 获取监测类型详情

**路径：** `GET /api/v1/monitor-types/{id}`\
**描述：** 获取监测类型详情及监测内容

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "JCLX001",
    "name": "雨量监测",
    "deviceType": 2,
    "deviceTypeName": "传感器",
    "icon": "/jc-icon/green/yl_green.png",
    "description": "雨量监测",
    "sortOrder": 0,
    "status": 1,
    "contents": [
      {
        "id": 1,
        "code": "rainfall_hour",
        "name": "小时雨量",
        "unit": "mm",
        "indicatorType": "yl",
        "rangeMin": 0,
        "rangeMax": 500
      }
    ]
  },
  "timestamp": 1735689213000
}
```

#### 3.3.4 新增监测类型

**路径：** `POST /api/v1/monitor-types`\
**描述：** 新增监测类型

**请求体：**

```json
{
  "code": "JCLX001",
  "name": "雨量监测",
  "deviceType": 2,
  "icon": "/jc-icon/green/yl_green.png",
  "description": "雨量监测类型",
  "sortOrder": 0,
  "status": 1
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 3.3.5 修改监测类型

**路径：** `PUT /api/v1/monitor-types/{id}`\
**描述：** 修改监测类型

**请求体：**

```json
{
  "name": "雨量监测修改",
  "deviceType": 2,
  "icon": "/jc-icon/green/yl_green.png",
  "description": "修改描述",
  "sortOrder": 10
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.3.6 删除监测类型

**路径：** `DELETE /api/v1/monitor-types/{id}`\
**描述：** 删除监测类型

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 3.4 监测内容管理

#### 3.4.1 获取监测内容列表

**路径：** `GET /api/v1/monitor-contents`\
**描述：** 获取监测内容列表

**请求参数：**

| 参数            | 类型   | 必填 | 说明     |
| ------------- | ---- | -- | ------ |
| monitorTypeId | long | 否  | 监测类型ID |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "monitorTypeId": 1,
      "monitorTypeName": "雨量监测",
      "code": "rainfall_hour",
      "name": "小时雨量",
      "unit": "mm",
      "indicatorType": "yl",
      "icon": "/jc-icon/green/yl_green.png",
      "rangeMin": 0,
      "rangeMax": 500
    }
  ],
  "timestamp": 1735689213000
}
```

#### 3.4.2 新增监测内容

**路径：** `POST /api/v1/monitor-contents`\
**描述：** 新增监测内容

**请求体：**

```json
{
  "monitorTypeId": 1,
  "code": "rainfall_hour",
  "name": "小时雨量",
  "unit": "mm",
  "indicatorType": "yl",
  "icon": "/jc-icon/green/yl_green.png",
  "rangeMin": 0,
  "rangeMax": 500
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 3.4.3 修改监测内容

**路径：** `PUT /api/v1/monitor-contents/{id}`\
**描述：** 修改监测内容

**请求体：**

```json
{
  "name": "小时雨量修改",
  "unit": "mm",
  "icon": "/jc-icon/green/yl_green.png",
  "rangeMin": 0,
  "rangeMax": 800
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.4.4 删除监测内容

**路径：** `DELETE /api/v1/monitor-contents/{id}`\
**描述：** 删除监测内容

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 3.5 设备管理

说明：

- 设备接口统一使用 `/api/v1/devices/*`
- 设备账号状态：`1-有效`，`2-禁用`
- 设备注册来源：`MANUAL / API / IMPORT`
- 设备接入协议：`MQTT / HTTP / COAP`
- 设备类型：`0-单参数`，`1-多参数`，`2-本地组网`
- 网络类型：`0-蜂窝`，`1-NB-Iot`
- 后台手工新增设备时，系统会自动生成 6 位设备用户名和 8 位设备密码

#### 3.5.0 权限要求

| 接口 | 权限标识 |
| --- | --- |
| `GET /api/v1/devices/page` | `basic:device:list` |
| `GET /api/v1/devices` | `basic:device:list` |
| `GET /api/v1/devices/{id}` | `basic:device:query` |
| `POST /api/v1/devices` | `basic:device:add` |
| `PUT /api/v1/devices/{id}` | `basic:device:edit` |
| `DELETE /api/v1/devices/{id}` | `basic:device:remove` |
| `POST /api/v1/devices/{id}/copy` | `basic:device:add` |
| `GET /api/v1/devices/{id}/sensors` | `basic:device:query` |
| `GET /api/v1/devices/{id}/auth-account` | `basic:device:auth:view` |
| `POST /api/v1/devices/{id}/auth-password/reset` | `basic:device:auth:reset` |
| `PUT /api/v1/devices/{id}/auth-status` | `basic:device:auth:status` |

#### 3.5.1 分页查询设备

**路径：** `GET /api/v1/devices/page`\
**描述：** 分页查询设备列表，返回设备基础信息、接入账号信息和最近鉴权摘要。

**请求参数：**

| 参数       | 类型     | 必填 | 说明 |
| -------- | ------ | -- | -- |
| pageNum  | int    | 否  | 页码 |
| pageSize | int    | 否  | 每页数量 |
| code     | string | 否  | 设备编号，模糊查询 |
| name     | string | 否  | 设备名称，模糊查询 |
| sn       | string | 否  | 设备SN，模糊查询 |
| status   | int    | 否  | 设备状态 |
| runStatus | int   | 否  | 运行状态 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "rows": [
      {
        "id": 1,
        "code": "DEVICE001",
        "name": "水位雷达",
        "sn": "SW-202605-0001",
        "deviceType": 0,
        "networkType": 0,
        "protocolType": "MQTT",
        "registerSource": "MANUAL",
        "vendorName": "中地厂商",
        "authUsername": "A1B2C3",
        "authPassword": "Ab12Cd34",
        "authStatus": 1,
        "icon": "sensor1",
        "iconPath": "/jc-icon/green/sensor1_green.png",
        "status": 1,
        "statusName": "正常",
        "runStatus": 1,
        "runStatusName": "运行中",
        "lastReportTime": "2026-05-28 18:10:00",
        "registeredAt": "2026-05-28 16:00:00",
        "lastAuthTime": "2026-05-28 18:09:58",
        "lastAuthIp": "10.10.10.12",
        "createTime": "2026-05-28 16:00:00"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1779963000000
}
```

#### 3.5.2 获取设备详情

**路径：** `GET /api/v1/devices/{id}`\
**描述：** 获取设备详情及设备下传感器列表。

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "DEVICE001",
    "name": "水位雷达",
    "sn": "SW-202605-0001",
    "deviceType": 0,
    "networkType": 0,
    "protocolType": "MQTT",
    "registerSource": "MANUAL",
    "vendorName": "中地厂商",
    "authUsername": "A1B2C3",
    "authPassword": "Ab12Cd34",
    "authStatus": 1,
    "icon": "sensor1",
    "iconPath": "/jc-icon/green/sensor1_green.png",
    "status": 1,
    "runStatus": 1,
    "lastReportTime": "2026-05-28 18:10:00",
    "registeredAt": "2026-05-28 16:00:00",
    "lastAuthTime": "2026-05-28 18:09:58",
    "lastAuthIp": "10.10.10.12",
    "createTime": "2026-05-28 16:00:00",
    "sensors": [
      {
        "id": 1,
        "sensorCode": "DEVICE001_1",
        "sensorNo": "1",
        "sensorName": "水位传感器",
        "monitorTypeId": 4,
        "monitorTypeCode": "JCLX004",
        "monitorTypeName": "水位监测",
        "status": 1,
        "attrList": [
          {
            "attrCode": "water_level",
            "attrName": "水位",
            "indicatorType": "sw",
            "indicatorTypeName": "水位",
            "initialValue": 0,
            "unit": "m",
            "rangeMin": 0,
            "rangeMax": 100
          }
        ]
      }
    ]
  },
  "timestamp": 1779963000000
}
```

#### 3.5.3 新增设备

**路径：** `POST /api/v1/devices`\
**描述：** 后台手工新增设备，并自动生成接入账号。

**请求体：**

```json
{
  "code": "DEVICE001",
  "name": "水位雷达",
  "sn": "SW-202605-0001",
  "deviceType": 0,
  "networkType": 0,
  "protocolType": "MQTT",
  "vendorName": "中地厂商",
  "icon": "sensor1",
  "iconPath": "/jc-icon/green/sensor1_green.png",
  "status": 1
}
```

**请求字段：**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | 是 | 设备编号，唯一 |
| name | string | 是 | 设备名称 |
| sn | string | 否 | 设备SN，传入时需唯一 |
| deviceType | int | 否 | `0单参数 / 1多参数 / 2本地组网` |
| networkType | int | 否 | `0蜂窝 / 1NB-Iot` |
| protocolType | string | 否 | `MQTT / HTTP / COAP`，默认 `MQTT` |
| vendorName | string | 否 | 厂商名称 |
| icon | string | 否 | 图标编码 |
| iconPath | string | 否 | 图标路径 |
| status | int | 是 | 设备状态 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1,
    "username": "A1B2C3",
    "password": "Ab12Cd34"
  },
  "timestamp": 1779963000000
}
```

#### 3.5.4 修改设备

**路径：** `PUT /api/v1/devices/{id}`\
**描述：** 修改设备基础信息，不修改设备编码和设备接入账号。

**请求体：**

```json
{
  "name": "水位雷达修改",
  "sn": "SW-202605-0001",
  "deviceType": 0,
  "networkType": 1,
  "protocolType": "HTTP",
  "vendorName": "中地厂商二期",
  "icon": "sensor2",
  "iconPath": "/jc-icon/green/sensor2_green.png",
  "status": 1
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": {
    "id": 1
  },
  "timestamp": 1779963000000
}
```

#### 3.5.5 删除设备

**路径：** `DELETE /api/v1/devices/{id}`\
**描述：** 逻辑删除设备，删除时会释放设备编码与接入账号占用。

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1779963000000
}
```

#### 3.5.6 复制设备

**路径：** `POST /api/v1/devices/{id}/copy`\
**描述：** 复制设备及其传感器配置，并为新设备重新生成接入账号。

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": 2,
  "timestamp": 1779963000000
}
```

#### 3.5.7 获取设备账号

**路径：** `GET /api/v1/devices/{id}/auth-account`\
**描述：** 查询设备接入账号信息。

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "deviceId": 1,
    "username": "A1B2C3",
    "password": "Ab12Cd34",
    "authStatus": 1,
    "registeredAt": "2026-05-28 16:00:00",
    "lastAuthTime": "2026-05-28 18:09:58",
    "lastAuthIp": "10.10.10.12"
  },
  "timestamp": 1779963000000
}
```

#### 3.5.8 重置设备密码

**路径：** `POST /api/v1/devices/{id}/auth-password/reset`\
**描述：** 重置设备接入密码，用户名保持不变。

**请求体：**

```json
{
  "reason": "现场更换设备",
  "forceOffline": true
}
```

**请求字段：**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| reason | string | 否 | 重置原因 |
| forceOffline | boolean | 否 | 预留字段，当前仅记录入参，不触发强制下线 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "重置成功",
  "data": {
    "username": "A1B2C3",
    "password": "Xy34Za89"
  },
  "timestamp": 1779963000000
}
```

#### 3.5.9 启用/禁用设备账号

**路径：** `PUT /api/v1/devices/{id}/auth-status`\
**描述：** 启用或禁用设备接入账号。

**请求体：**

```json
{
  "authStatus": 2,
  "reason": "设备退场停用"
}
```

**请求字段：**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| authStatus | int | 是 | `1-有效`，`2-禁用` |
| reason | string | 否 | 状态变更原因 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "状态更新成功",
  "data": {
    "deviceId": 1,
    "username": "A1B2C3",
    "password": "Ab12Cd34",
    "authStatus": 2,
    "registeredAt": "2026-05-28 16:00:00",
    "lastAuthTime": "2026-05-28 18:09:58",
    "lastAuthIp": "10.10.10.12"
  },
  "timestamp": 1779963000000
}
```

#### 3.5.10 MQTT 设备接入约定

**类型：** 非 HTTP 接口规范\
**描述：** 说明设备通过 MQTT Broker 接入平台时的鉴权参数、主题约束与运行规则，供设备端、联调端与测试端统一参考。

**Broker 连接参数：**

| 项 | 值 | 说明 |
| --- | --- | --- |
| 协议 | MQTT | 基于 `mica-mqtt` 服务端实现 |
| TCP 端口 | `1883` | `mqtt.server.mqtt-listener.port` |
| WebSocket 端口 | `8083` | `mqtt.server.ws-listener.port` |
| HTTP 管理端口 | `18083` | Broker 内置 HTTP listener |
| 鉴权开关 | 开启 | `mqtt.server.auth.enable = true` |

**鉴权参数：**

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `clientId` | 是 | 设备连接标识，建议使用 `device-{deviceId}` 或可追踪的唯一标识 |
| `username` | 是 | 平台生成的 6 位设备用户名 |
| `password` | 是 | 平台生成的 8 位设备密码 |

**CONNECT 鉴权规则：**

1. 根据 `username` 查询设备。
2. 校验设备账号状态 `authStatus = 1`。
3. 直接比对明文密码。
4. 当 `mqtt.auth-center.enforce-mqtt-protocol = true` 时，设备 `protocolType` 必须为 `MQTT`。
5. 鉴权成功后更新 `device.last_auth_time`、`device.last_auth_ip`、`device.run_status`。
6. 鉴权结果写入 `device_auth_log`。
7. 同一设备只允许 1 个活跃连接；当 `mqtt.auth-center.disconnect-previous-client = true` 时，重复登录会挤掉旧连接。
8. 连续鉴权失败达到 `mqtt.auth-center.failure-threshold` 后进入临时封禁，封禁时长由 `mqtt.auth-center.ban-duration-seconds` 控制。

**发布主题规则：**

| 主题                                      | 说明           |
|-----------------------------------------|--------------|
| `sys/v1/{deviceCode}/{sensorNo}/updata` | 通用 JSON 上报主题 |
| `gb/v1/{deviceCode}/{sensorNo}/updata`  | 国标兼容上报主题     |

**发布校验规则：**

1. 仅允许发布上述两类主题。
2. `topic` 中的 `{deviceCode}` 必须与当前已鉴权设备的 `code` 字段一致（通过 `MqttDeviceSession.deviceCode()` 直接比对，无需查库）。
3. 传感器存在性与启用状态校验下沉至 `MonitorMetadataService.requireSensorMetadata()` 在数据接入阶段统一执行。
4. 校验不通过时 Broker 返回拒绝，服务端记录结构化 MQTT 异常日志。

**订阅校验规则：**

| 主题 | 说明 |
| --- | --- |
| `sys/v1/{deviceCode}/{sensorCode}/updata` | 当前订阅校验实现允许的设备订阅主题 |

说明：

- 订阅主题需满足格式校验和数据库存在性校验。
- 当前 MQTT 鉴权中心已实现鉴权与权限控制；消息解析、IoTDB 落库、告警联动不属于本节范围。

#### 3.5.10 设备注册中心

**路径：** `POST /api/v1/device-registry/register`\
**描述：** 匿名设备注册接口，按 `requestId` 幂等处理，按 `sn` 去重，自动发放设备账号并生成传感器及默认属性。

**鉴权：** 匿名接口，不需要 `Authorization` 头。

**请求体：**

```json
{
  "requestId": "REQ-20260528-0001",
  "registerCode": "ABCDEF123456",
  "vendorName": "中地厂商",
  "sn": "SW-202605-0001",
  "deviceName": "GNSS位移监测仪",
  "deviceType": "0",
  "network": "0",
  "protocol": "0",
  "monitorTypes": [
    {
      "type": "L1_LF",
      "sid": "1"
    }
  ],
  "childDevices": [
    {
      "sn": "SW-202605-0001-A",
      "deviceName": "裂缝子机A",
      "monitorTypes": [
        {
          "type": "L1_LF",
          "sid": "1"
        }
      ]
    }
  ]
}
```

**请求字段：**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| requestId | string | 是 | 幂等请求ID |
| registerCode | string | 是 | 设备注册码 |
| vendorName | string | 否 | 厂商名称 |
| sn | string | 是 | 主设备SN |
| deviceName | string | 是 | 主设备名称 |
| deviceType | string | 是 | `0 / 1 / 2`，字符串传输 |
| network | string | 是 | `0 / 1`，字符串传输 |
| protocol | string | 是 | `0 / 1 / 2` 或 `MQTT / HTTP / COAP` |
| monitorTypes | array | 是 | 主设备监测类型列表，至少 1 项 |
| monitorTypes[].type | string | 是 | 监测类型编码 |
| monitorTypes[].sid | string | 是 | 传感器编号 |
| childDevices | array | 否 | 本地组网子设备列表 |
| childDevices[].sn | string | 是 | 子设备SN |
| childDevices[].deviceName | string | 是 | 子设备名称 |
| childDevices[].monitorTypes | array | 是 | 子设备监测类型列表，至少 1 项 |

**成功响应（新建设备）：**

```json
{
  "code": 200,
  "msg": "注册成功",
  "data": {
    "deviceId": 101,
    "username": "A1B2C3",
    "password": "Ab12Cd34",
    "created": true
  },
  "timestamp": 1779963000000
}
```

**成功响应（命中幂等或已有同SN设备）：**

```json
{
  "code": 200,
  "msg": "注册成功",
  "data": {
    "deviceId": 101,
    "username": "A1B2C3",
    "password": "Ab12Cd34",
    "created": false
  },
  "timestamp": 1779963000000
}
```

**失败响应示例（注册码无效）：**

```json
{
  "code": 401,
  "msg": "注册码无效",
  "timestamp": 1779963000000
}
```

**失败响应示例（同 requestId 的失败记录或 SN 信息不一致）：**

```json
{
  "code": 409,
  "msg": "设备SN已存在但请求信息不一致",
  "timestamp": 1779963000000
}
```

### 3.6 传感器管理

#### 3.6.1 获取设备传感器

**路径：** `GET /api/v1/devices/{deviceId}/sensors`\
**描述：** 获取设备下的传感器列表

**路径参数：**

| 参数       | 类型   | 必填 | 说明   |
| -------- | ---- | -- | ---- |
| deviceId | long | 是  | 设备ID |

**业务说明：**

- 返回指定设备下所有未删除的传感器
- 每个传感器节点会附带 `attrList` 属性列表
- 属性列表为当前传感器的完整属性快照

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "sensorCode": "SENSOR001",
      "sensorNo": "1",
      "sensorName": "水位传感器",
      "monitorTypeId": 4,
      "monitorTypeCode": "JCLX004",
      "monitorTypeName": "水位监测",
      "status": 1,
      "attrList": [
        {
          "attrCode": "water_level",
          "attrName": "水位",
          "indicatorType": "sw",
          "indicatorTypeName": "水位",
          "initialValue": 0,
          "unit": "m",
          "rangeMin": 0,
          "rangeMax": 100
        }
      ]
    }
  ],
  "timestamp": 1735689213000
}
```

#### 3.6.2 获取传感器详情

**路径：** `GET /api/v1/sensors/{id}`\
**描述：** 获取单个传感器详情

**路径参数：**

| 参数 | 类型   | 必填 | 说明    |
| -- | ---- | -- | ----- |
| id | long | 是  | 传感器ID |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "deviceId": 1,
    "deviceCode": "DEVICE001",
    "sensorCode": "SENSOR001",
    "sensorNo": "1",
    "sensorName": "水位传感器",
    "monitorTypeId": 4,
    "monitorTypeCode": "JCLX004",
    "monitorTypeName": "水位监测",
    "status": 1,
    "createTime": "2024-01-10 10:00:00",
    "updateTime": "2024-01-20 14:30:00",
    "attrList": [
      {
        "id": 1,
        "attrCode": "water_level",
        "attrName": "水位",
        "indicatorType": "sw",
        "indicatorTypeName": "水位",
        "initialValue": 0,
        "unit": "m",
        "rangeMin": 0,
        "rangeMax": 100,
        "icon": "/jc-icon/green/dw_green.png"
      }
    ]
  },
  "timestamp": 1735689213000
}
```

#### 3.6.3 新增传感器

**路径：** `POST /api/v1/devices/{deviceId}/sensors`\
**描述：** 为设备新增传感器

**路径参数：**

| 参数       | 类型   | 必填 | 说明   |
| -------- | ---- | -- | ---- |
| deviceId | long | 是  | 设备ID |

**请求体：**

```json
{
  "sensorCode": "SENSOR001",
  "sensorNo": "1",
  "sensorName": "水位传感器",
  "monitorTypeId": 4,
  "status": 1,
  "attrList": [
    {
      "attrCode": "water_level",
      "attrName": "水位",
      "indicatorType": "sw",
      "initialValue": 0,
      "unit": "m",
      "rangeMin": 0,
      "rangeMax": 100
    }
  ]
}
```

**请求约束：**

| 参数            | 类型      | 必填 | 说明 |
| ------------- | ------- | -- | -- |
| sensorCode    | string  | 是  | 传感器编号，长度不超过100 |
| sensorNo      | string  | 否  | 传感器主题编号；不传时默认与 `sensorCode` 一致 |
| sensorName    | string  | 是  | 传感器名称，长度不超过200 |
| monitorTypeId | long    | 是  | 监测类型ID |
| status        | int     | 是  | 状态，`0-禁用`，`1-启用` |
| attrList      | array   | 是  | 属性列表，至少1项 |
| attrList[].attrCode | string | 是 | 属性编码，长度不超过100 |
| attrList[].attrName | string | 是 | 属性名称，长度不超过200 |
| attrList[].indicatorType | string | 否 | 指标类型，长度不超过50 |
| attrList[].indicatorTypeName | string | 否 | 指标类型名称，长度不超过100 |
| attrList[].initialValue | decimal(12,2) | 否 | 初始值 |
| attrList[].unit | string | 否 | 单位，长度不超过50 |
| attrList[].rangeMin | decimal(12,2) | 否 | 最小值 |
| attrList[].rangeMax | decimal(12,2) | 否 | 最大值 |
| attrList[].icon | string | 否 | 图标路径，长度不超过500 |

**业务规则：**

- `deviceId` 对应设备必须存在
- `sensorCode` 在未删除传感器范围内必须唯一
- `monitorTypeId` 对应监测类型必须存在
- 仅允许选择 `deviceType = 2` 的监测类型
- `attrList` 不能为空
- 同一传感器下 `attrCode` 不允许重复
- 当同时传入 `rangeMin` 与 `rangeMax` 时，`rangeMin` 不能大于 `rangeMax`
- 后端会根据 `deviceId` 和 `monitorTypeId` 自动回填 `deviceCode`、`monitorTypeCode`、`monitorTypeName`

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 3.6.4 修改传感器

**路径：** `PUT /api/v1/sensors/{id}`\
**描述：** 修改传感器

**路径参数：**

| 参数 | 类型   | 必填 | 说明    |
| -- | ---- | -- | ----- |
| id | long | 是  | 传感器ID |

**请求体：**

```json
{
  "sensorName": "水位传感器修改",
  "status": 1,
  "attrList": [
    {
      "id": 1,
      "attrCode": "water_level",
      "attrName": "水位",
      "indicatorType": "sw",
      "indicatorTypeName": "水位",
      "initialValue": 10,
      "unit": "m",
      "rangeMin": 0,
      "rangeMax": 100
    }
  ]
}
```

**请求约束：**

| 参数            | 类型      | 必填 | 说明 |
| ------------- | ------- | -- | -- |
| sensorName    | string  | 是  | 传感器名称，长度不超过200 |
| status        | int     | 是  | 状态，`0-禁用`，`1-启用` |
| attrList      | array   | 是  | 属性列表，至少1项 |
| attrList[].id | long    | 否  | 属性ID，已有属性更新时传入；新增属性可不传 |
| attrList[].attrCode | string | 是 | 属性编码，长度不超过100 |
| attrList[].attrName | string | 是 | 属性名称，长度不超过200 |
| attrList[].indicatorType | string | 否 | 指标类型，长度不超过50 |
| attrList[].indicatorTypeName | string | 否 | 指标类型名称，长度不超过100 |
| attrList[].initialValue | decimal(12,2) | 否 | 初始值 |
| attrList[].unit | string | 否 | 单位，长度不超过50 |
| attrList[].rangeMin | decimal(12,2) | 否 | 最小值 |
| attrList[].rangeMax | decimal(12,2) | 否 | 最大值 |
| attrList[].icon | string | 否 | 图标路径，长度不超过500 |

**业务规则：**

- `id` 对应传感器必须存在
- 当前接口仅允许修改 `sensorName`、`status` 和 `attrList`
- `sensorCode`、`deviceId`、`monitorTypeId` 不在本接口中修改
- `attrList` 不能为空
- 同一传感器下 `attrCode` 不允许重复
- 当 `attrList[].id` 存在时按原属性更新
- 当 `attrList[].id` 不存在时按新增属性处理
- 原有属性中未出现在本次 `attrList` 中的记录会被删除
- 当同时传入 `rangeMin` 与 `rangeMax` 时，`rangeMin` 不能大于 `rangeMax`

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.6.5 删除传感器

**路径：** `DELETE /api/v1/sensors/{id}`\
**描述：** 删除传感器

**路径参数：**

| 参数 | 类型   | 必填 | 说明    |
| -- | ---- | -- | ----- |
| id | long | 是  | 传感器ID |

**业务说明：**

- 删除时会先删除该传感器下的所有属性记录
- 传感器主记录采用逻辑删除
- 逻辑删除时会重写 `sensorCode`，释放唯一键占用

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 3.7 视频设备管理

#### 3.7.1 分页查询视频设备

**路径：** `GET /api/v1/video-devices/page`\
**描述：** 分页查询视频设备

**请求参数：**

| 参数           | 类型     | 必填 | 说明   |
| ------------ | ------ | -- | ---- |
| pageNum      | int    | 否  | 页码   |
| pageSize     | int    | 否  | 每页数量 |
| code         | string | 否  | 设备编号 |
| name         | string | 否  | 设备名称 |
| protocolCode | string | 否  | 协议类型 |
| status       | int    | 否  | 状态   |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 50,
    "rows": [
      {
        "id": 1,
        "code": "VD001",
        "name": "隐患点A摄像头",
        "icon": "vidio1",
        "iconPath": "/jc-icon/green/vidio1_green.png",
        "protocolCode": "RTSP",
        "protocolName": "RTSP",
        "streamUrl": "rtsp://192.168.1.101:554/stream",
        "status": 1,
        "statusName": "在线",
        "lastOnlineTime": "2024-01-20 14:30:00",
        "installTime": "2024-01-10 10:00:00",
        "createTime": "2024-01-10 10:00:00"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 3.7.2 获取视频设备详情

**路径：** `GET /api/v1/video-devices/{id}`\
**描述：** 获取视频设备详情

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "VD001",
    "name": "隐患点A摄像头",
    "icon": "vidio1",
    "iconPath": "/jc-icon/green/vidio1_green.png",
    "protocolCode": "RTSP",
    "protocolName": "RTSP",
    "streamUrl": "rtsp://192.168.1.101:554/stream",
    "status": 1,
    "lastOnlineTime": "2024-01-20 14:30:00",
    "installTime": "2024-01-10 10:00:00",
    "createTime": "2024-01-10 10:00:00"
  },
  "timestamp": 1735689213000
}
```

#### 3.7.3 新增视频设备

**路径：** `POST /api/v1/video-devices`\
**描述：** 新增视频设备

**请求体：**

```json
{
  "code": "VD001",
  "name": "隐患点A摄像头",
  "icon": "vidio1",
  "iconPath": "/jc-icon/green/vidio1_green.png",
  "protocolCode": "RTSP",
  "protocolName": "RTSP",
  "streamUrl": "rtsp://192.168.1.101:554/stream",
  "installTime": "2024-01-10 10:00:00"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 3.7.4 修改视频设备

**路径：** `PUT /api/v1/video-devices/{id}`\
**描述：** 修改视频设备

**请求体：**

```json
{
  "name": "隐患点A摄像头修改",
  "icon": "vidio2",
  "iconPath": "/jc-icon/green/vidio2_green.png",
  "protocolCode": "RTMP",
  "protocolName": "RTMP",
  "streamUrl": "rtmp://192.168.1.101:1935/live/stream"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 3.7.5 删除视频设备

**路径：** `DELETE /api/v1/video-devices/{id}`\
**描述：** 删除视频设备

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

***

## 4. 设备绑定接口

### 4.1 设备隐患点绑定

#### 4.1.1 获取隐患点绑定的设备

**路径：** `GET /api/v1/hazard-points/{hpId}/bound-devices`\
**描述：** 获取隐患点已绑定的设备列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "deviceId": 1,
      "deviceCode": "DEVICE001",
      "deviceName": "水位雷达",
      "bindTime": "2024-01-15 10:00:00",
      "installLongitude": 104.156789,
      "installLatitude": 30.678901,
      "deviceStatus": 1,
      "sensors": [
        {
          "id": 1,
          "name": "水位传感器",
          "iconPath": "/jc-icon/green/sensor1_green.png"
        }
      ]
    }
  ],
  "timestamp": 1735689213000
}
```

#### 4.1.2 获取未绑定设备列表

**路径：** `GET /api/v1/hazard-points/{hpId}/unbound-devices`\
**描述：** 获取隐患点未绑定的设备列表

**请求参数：**

| 参数      | 类型     | 必填 | 说明           |
| ------- | ------ | -- | ------------ |
| keyword | string | 否  | 设备/传感器名称模糊查询 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "label": "DEVICE001 - 水位雷达",
      "bindCount": 2,
      "status": 1,
      "children": [
        {
          "id": 101,
          "label": "水位传感器",
          "iconPath": "/jc-icon/green/sensor1_green.png"
        }
      ]
    }
  ],
  "timestamp": 1735689213000
}
```

#### 4.1.3 绑定设备到隐患点

**路径：** `POST /api/v1/hazard-points/{hpId}/bind-devices`\
**描述：** 绑定设备到隐患点

**请求体：**

```json
{
  "deviceIds": [1, 2],
  "installPositions": [
    {
      "deviceId": 1,
      "installLongitude": 104.156789,
      "installLatitude": 30.678901
    }
  ]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "绑定成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 4.1.4 解绑设备

**路径：** `DELETE /api/v1/hazard-points/{hpId}/unbind-devices`\
**描述：** 从隐患点解绑设备

**请求体：**

```json
{
  "deviceIds": [1, 2]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "解绑成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 4.2 视频设备隐患点绑定

#### 4.2.1 获取隐患点绑定的视频设备

**路径：** `GET /api/v1/hazard-points/{hpId}/bound-video-devices`\
**描述：** 获取隐患点已绑定的视频设备

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "videoDeviceId": 1,
      "deviceCode": "VD001",
      "deviceName": "摄像头1",
      "bindTime": "2024-01-15 10:00:00",
      "installLongitude": 104.156789,
      "installLatitude": 30.678901
    }
  ],
  "timestamp": 1735689213000
}
```

#### 4.2.2 绑定视频设备到隐患点

**路径：** `POST /api/v1/hazard-points/{hpId}/bind-video-devices`\
**描述：** 绑定视频设备到隐患点

**请求体：**

```json
{
  "videoDeviceIds": [1, 2],
  "installPositions": [
    {
      "videoDeviceId": 1,
      "installLongitude": 104.156789,
      "installLatitude": 30.678901
    }
  ]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "绑定成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 4.2.3 解绑视频设备

**路径：** `DELETE /api/v1/hazard-points/{hpId}/unbind-video-devices`\
**描述：** 从隐患点解绑视频设备

**请求体：**

```json
{
  "videoDeviceIds": [1, 2]
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "解绑成功",
  "data": null,
  "timestamp": 1735689213000
}
```

***

## 5. 告警中心模块接口

### 5.1 告警判据管理

#### 5.1.1 获取隐患点告警判据

**路径：** `GET /api/v1/hazard-points/{hpId}/alarm-criteria`\
**描述：** 获取隐患点的告警判据列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "name": "水位告警判据",
      "deviceId": 1,
      "deviceName": "水位雷达",
      "monitorTypeId": 4,
      "monitorTypeName": "水位监测",
      "blueExpression": "water_level >= 80",
      "blueDescription": "超出设防水位",
      "yellowExpression": "water_level >= 85",
      "yellowDescription": "超出警戒水位",
      "orangeExpression": "water_level >= 90",
      "orangeDescription": "超出警戒水位",
      "redExpression": "water_level >= 95",
      "redDescription": "超出保证水位",
      "isEnabled": true,
      "createTime": "2024-01-15 10:00:00"
    }
  ],
  "timestamp": 1735689213000
}
```

#### 5.1.2 新增告警判据

**路径：** `POST /api/v1/hazard-points/{hpId}/alarm-criteria`\
**描述：** 为隐患点新增告警判据

**请求体：**

```json
{
  "name": "水位告警判据",
  "deviceId": 1,
  "monitorTypeId": 4,
  "blueExpression": "water_level >= 80",
  "blueDescription": "超出设防水位",
  "yellowExpression": "water_level >= 85",
  "yellowDescription": "超出警戒水位",
  "orangeExpression": "water_level >= 90",
  "orangeDescription": "超出警戒水位",
  "redExpression": "water_level >= 95",
  "redDescription": "超出保证水位"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 5.1.3 修改告警判据

**路径：** `PUT /api/v1/alarm-criteria/{id}`\
**描述：** 修改告警判据

**请求体：**

```json
{
  "name": "水位告警判据修改",
  "blueExpression": "water_level >= 82",
  "blueDescription": "超出设防水位(修改)",
  "yellowExpression": "water_level >= 87",
  "yellowDescription": "超出警戒水位(修改)"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 5.1.4 删除告警判据

**路径：** `DELETE /api/v1/alarm-criteria/{id}`\
**描述：** 删除告警判据

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 5.1.5 启用/禁用告警判据

**路径：** `PUT /api/v1/alarm-criteria/{id}/toggle`\
**描述：** 启用或禁用告警判据

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 5.2 告警分发规则

#### 5.2.1 获取隐患点分发规则

**路径：** `GET /api/v1/hazard-points/{hpId}/dispatch-rules`\
**描述：** 获取隐患点的告警分发规则列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "name": "告警分发规则1",
      "type": 1,
      "typeName": "告警分发",
      "alarmLevel": "1,2,3,4",
      "recipientIds": [1, 2],
      "recipientName": "管理员,操作员",
      "channel": "SYSTEM,SMS",
      "isEnabled": true,
      "createTime": "2024-01-15 10:00:00"
    },
    {
      "id": 2,
      "name": "离线通知规则",
      "type": 2,
      "typeName": "状态通知",
      "deviceId": 1,
      "timeSetting": "定时",
      "timeValue": "9:30,16:00",
      "recipientIds": [1],
      "recipientName": "管理员",
      "channel": "SYSTEM",
      "isEnabled": true,
      "createTime": "2024-01-15 10:00:00"
    }
  ],
  "timestamp": 1735689213000
}
```

#### 5.2.2 新增分发规则

**路径：** `POST /api/v1/hazard-points/{hpId}/dispatch-rules`\
**描述：** 为隐患点新增告警分发规则

**请求体（告警分发）：**

```json
{
  "name": "告警分发规则",
  "type": 1,
  "alarmLevel": "1,2,3,4",
  "recipientIds": [1, 2],
  "channel": "SYSTEM,SMS"
}
```

**请求体（状态通知）：**

```json
{
  "name": "离线通知规则",
  "type": 2,
  "deviceId": 1,
  "timeSetting": "定时",
  "timeValue": "9:30,16:00",
  "recipientIds": [1],
  "channel": "SYSTEM"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 5.2.3 修改分发规则

**路径：** `PUT /api/v1/dispatch-rules/{id}`\
**描述：** 修改告警分发规则

**请求体：**

```json
{
  "name": "规则修改",
  "alarmLevel": "2,3,4",
  "recipientIds": [1, 2, 3],
  "channel": "SYSTEM,SMS,WECHAT"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 5.2.4 删除分发规则

**路径：** `DELETE /api/v1/dispatch-rules/{id}`\
**描述：** 删除告警分发规则

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 5.2.5 启用/禁用分发规则

**路径：** `PUT /api/v1/dispatch-rules/{id}/toggle`\
**描述：** 启用或禁用告警分发规则

**成功响应：**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 5.3 告警记录

#### 5.3.1 分页查询告警记录

**路径：** `GET /api/v1/alarm-records/page`\
**描述：** 分页查询告警记录

**请求参数：**

| 参数            | 类型     | 必填 | 说明    |
| ------------- | ------ | -- | ----- |
| pageNum       | int    | 否  | 页码    |
| pageSize      | int    | 否  | 每页数量  |
| hazardPointId | long   | 否  | 隐患点ID |
| alarmLevel    | int    | 否  | 告警等级  |
| status        | int    | 否  | 处理状态  |
| startTime     | string | 否  | 开始时间  |
| endTime       | string | 否  | 结束时间  |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "rows": [
      {
        "id": 1,
        "hazardPointId": 1,
        "hazardPointCode": "HP001",
        "hazardPointName": "隐患点A",
        "alarmLevel": 2,
        "alarmLevelText": "二级(黄色)",
        "alarmType": "水位告警",
        "alarmMessage": "水位超出警戒水位",
        "deviceId": 1,
        "deviceCode": "DEVICE001",
        "deviceName": "水位雷达",
        "monitorContent": "水位",
        "currentValue": 86.5,
        "thresholdValue": 85,
        "status": 1,
        "statusName": "待处理",
        "createTime": "2024-01-20 14:30:00"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 5.3.2 获取告警详情

**路径：** `GET /api/v1/alarm-records/{id}`\
**描述：** 获取告警记录详情

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "hazardPointId": 1,
    "hazardPointCode": "HP001",
    "hazardPointName": "隐患点A",
    "alarmLevel": 2,
    "alarmLevelText": "二级(黄色)",
    "alarmType": "水位告警",
    "alarmMessage": "水位超出警戒水位",
    "deviceId": 1,
    "deviceCode": "DEVICE001",
    "deviceName": "水位雷达",
    "monitorContent": "水位",
    "currentValue": 86.5,
    "thresholdValue": 85,
    "status": 1,
    "statusName": "待处理",
    "createTime": "2024-01-20 14:30:00",
    "handleTime": null,
    "handlePerson": null,
    "handleResult": null
  },
  "timestamp": 1735689213000
}
```

#### 5.3.3 处理告警

**路径：** `PUT /api/v1/alarm-records/{id}/handle`\
**描述：** 处理告警记录

**请求体：**

```json
{
  "status": 3,
  "handleResult": "已派人前往现场查看，情况正常"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "处理成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 5.3.4 批量处理告警

**路径：** `PUT /api/v1/alarm-records/batch/handle`\
**描述：** 批量处理告警记录

**请求体：**

```json
{
  "ids": [1, 2, 3],
  "status": 3,
  "handleResult": "批量处理"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "批量处理成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 5.3.5 获取告警统计

**路径：** `GET /api/v1/alarm-records/statistics`\
**描述：** 获取告警统计信息

**请求参数：**

| 参数        | 类型     | 必填 | 说明   |
| --------- | ------ | -- | ---- |
| startTime | string | 否  | 开始时间 |
| endTime   | string | 否  | 结束时间 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "level1": 10,
    "level2": 30,
    "level3": 40,
    "level4": 20,
    "pending": 20,
    "processing": 10,
    "handled": 60,
    "ignored": 10
  },
  "timestamp": 1735689213000
}
```

***

## 6. 监测数据模块接口

> **接口实现说明（截至 2026-06-02）：**
> - `GET /api/v1/monitor-data/chart`、`page`、`latest` 三个查询接口已实现，查询源统一为 IoTDB，不再查 MySQL `monitor_data`
>    表。
> - `page` 接口已支持 IoTDB 层 `LIMIT/OFFSET` 分页，替代内存分页。
> - `chart`、`page` 接口已支持 `valueType` 聚合查询（`current`/`hour`/`24h`/`72h`）。
> - `chart` 接口已支持多测点返回多序列数据。
> - `export`/`import` 接口本期未实现，暂不落地。
> - 告警引擎（`alarm-criteria`、`alarm-records`）本期未实现，待后续迭代。

### 6.1 监测数据查询

#### 6.1.1 查询监测数据（图表）

**路径：** `GET /api/v1/monitor-data/chart`\
**描述：** 查询监测数据用于图表展示

**请求参数：**

| 参数            | 类型     | 必填 | 说明                       |
| ------------- | ------ | -- | ------------------------ |
| hazardPointId | long   | 是  | 隐患点ID                    |
| deviceId      | long   | 否  | 设备ID                     |
| sensorId      | long   | 否  | 传感器ID                    |
| attrCode      | string | 否  | 属性编码                     |
| valueType     | string | 否  | 值类型：current/hour/24h/72h |
| startTime     | string | 是  | 开始时间                     |
| endTime       | string | 是  | 结束时间                     |

**成功响应（单测点，向后兼容）：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "seriesName": "小时雨量 (水位传感器)",
      "deviceName": "水位雷达",
      "sensorName": "水位传感器",
      "labels": ["00:00", "01:00", "02:00", "..."],
      "values": [10.5, 12.3, 11.8, "..."],
      "unit": "mm",
      "attrName": "小时雨量",
      "maxValue": 15.2,
      "minValue": 8.5,
      "avgValue": 11.2
    }
  ],
  "timestamp": 1735689213000
}
```

> **说明：** 当筛选条件匹配多个测点时， 数组包含多个序列对象，每个序列对应一个测点指标。

#### 6.1.2 查询监测数据（表格）

**路径：** `GET /api/v1/monitor-data/page`\
**描述：** 分页查询监测数据

**请求参数：**

| 参数            | 类型     | 必填 | 说明                       |
| ------------- | ------ | -- | ------------------------ |
| pageNum       | int    | 否  | 页码，默认1                   |
| pageSize      | int    | 否  | 每页数量，默认10                |
| hazardPointId | long   | 是  | 隐患点ID                    |
| deviceId      | long   | 否  | 设备ID                     |
| sensorId      | long   | 否  | 传感器ID                    |
| attrCode      | string | 否  | 属性编码                     |
| valueType     | string | 否  | 值类型：current/hour/24h/72h |
| startTime     | string | 否  | 开始时间                     |
| endTime       | string | 否  | 结束时间                     |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 1000,
    "rows": [
      {
        "hazardPointId": 1,
        "hazardPointName": "隐患点A",
        "deviceId": 1,
        "deviceName": "水位雷达",
        "sensorId": 1,
        "sensorName": "水位传感器",
        "attrCode": "water_level",
        "attrName": "水位",
        "value": 86.5,
        "unit": "m",
        "dataTime": "2024-01-20 14:30:00",
        "quality": 0,
        "qualityText": "正常"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 6.1.3 获取最新监测数据

**路径：** `GET /api/v1/monitor-data/latest`\
**描述：** 获取隐患点最新监测数据

**请求参数：**

| 参数            | 类型   | 必填 | 说明    |
| ------------- | ---- | -- | ----- |
| hazardPointId | long | 是  | 隐患点ID |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "hazardPointId": 1,
      "hazardPointName": "隐患点A",
      "deviceId": 1,
      "deviceName": "水位雷达",
      "sensorId": 1,
      "sensorName": "水位传感器",
      "attrCode": "water_level",
      "attrName": "水位",
      "value": 86.5,
      "unit": "m",
      "dataTime": "2024-01-20 14:30:00",
      "quality": 0,
      "qualityText": "正常"
    }
  ],
  "timestamp": 1735689213000
}
```

### 6.2 数据导入导出

#### 6.2.1 导出监测数据

**路径：** `GET /api/v1/monitor-data/export`\
**描述：** 导出监测数据

**请求参数：**

| 参数            | 类型     | 必填 | 说明    |
| ------------- | ------ | -- | ----- |
| hazardPointId | long   | 是  | 隐患点ID |
| startTime     | string | 否  | 开始时间  |
| endTime       | string | 否  | 结束时间  |

**成功响应：** 文件流

#### 6.2.2 导入监测数据

**路径：** `POST /api/v1/monitor-data/import`\
**描述：** 导入监测数据

**请求参数：**

| 参数   | 类型            | 必填 | 说明      |
| ---- | ------------- | -- | ------- |
| file | MultipartFile | 是  | Excel文件 |

**成功响应：**

```json
{
  "code": 200,
  "msg": "导入成功",
  "data": {
    "total": 100,
    "success": 98,
    "failed": 2
  },
  "timestamp": 1735689213000
}
```

***

## 7. 报告报表模块接口

### 7.1 报告模板

#### 7.1.1 获取报告模板列表

**路径：** `GET /api/v1/report-templates`\
**描述：** 获取报告模板列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "code": "DAILY",
      "name": "日报模板",
      "type": 1,
      "typeName": "日报",
      "description": "每日监测报告模板",
      "sortOrder": 0,
      "status": 1
    }
  ],
  "timestamp": 1735689213000
}
```

#### 7.1.2 新增报告模板

**路径：** `POST /api/v1/report-templates`\
**描述：** 新增报告模板

**请求体：**

```json
{
  "code": "DAILY",
  "name": "日报模板",
  "type": 1,
  "description": "每日监测报告模板",
  "content": "<html>...</html>",
  "params": "{...}"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "新增成功",
  "data": {
    "id": 1
  },
  "timestamp": 1735689213000
}
```

#### 7.1.3 修改报告模板

**路径：** `PUT /api/v1/report-templates/{id}`\
**描述：** 修改报告模板

**请求体：**

```json
{
  "name": "日报模板修改",
  "content": "<html>...</html>"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null,
  "timestamp": 1735689213000
}
```

#### 7.1.4 删除报告模板

**路径：** `DELETE /api/v1/report-templates/{id}`\
**描述：** 删除报告模板

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

### 7.2 报告记录

#### 7.2.1 分页查询报告记录

**路径：** `GET /api/v1/report-records/page`\
**描述：** 分页查询报告记录

**请求参数：**

| 参数            | 类型     | 必填 | 说明    |
| ------------- | ------ | -- | ----- |
| pageNum       | int    | 否  | 页码    |
| pageSize      | int    | 否  | 每页数量  |
| templateId    | long   | 否  | 模板ID  |
| hazardPointId | long   | 否  | 隐患点ID |
| reportDate    | string | 否  | 报告日期  |
| status        | int    | 否  | 状态    |

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "rows": [
      {
        "id": 1,
        "templateId": 1,
        "templateName": "日报模板",
        "hazardPointId": 1,
        "hazardPointCode": "HP001",
        "hazardPointName": "隐患点A",
        "reportName": "2024-01-20 日报",
        "reportDate": "2024-01-20 00:00:00",
        "filePath": "/reports/20240120_HP001_daily.pdf",
        "status": 2,
        "statusName": "已生成",
        "createTime": "2024-01-20 08:00:00"
      }
    ],
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1735689213000
}
```

#### 7.2.2 生成报告

**路径：** `POST /api/v1/report-records/generate`\
**描述：** 生成报告

**请求体：**

```json
{
  "templateId": 1,
  "hazardPointId": 1,
  "reportDate": "2024-01-20"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "生成成功",
  "data": {
    "id": 1,
    "filePath": "/reports/20240120_HP001_daily.pdf"
  },
  "timestamp": 1735689213000
}
```

#### 7.2.3 下载报告

**路径：** `GET /api/v1/report-records/{id}/download`\
**描述：** 下载报告文件

**成功响应：** 文件流

#### 7.2.4 删除报告

**路径：** `DELETE /api/v1/report-records/{id}`\
**描述：** 删除报告记录

**成功响应：**

```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null,
  "timestamp": 1735689213000
}
```

***

## 8. 数据字典接口

### 8.1 获取字典类型列表

**路径：** `GET /api/v1/dict-types`\
**描述：** 获取所有字典类型列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "code": "ALARM_LEVEL",
      "name": "告警等级",
      "description": "告警等级字典"
    }
  ],
  "timestamp": 1735689213000
}
```

### 8.2 获取字典数据

**路径：** `GET /api/v1/dict-data/{typeCode}`\
**描述：** 根据字典类型编码获取字典数据列表

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "typeCode": "ALARM_LEVEL",
      "value": "1",
      "label": "一级(蓝色)"
    },
    {
      "id": 2,
      "typeCode": "ALARM_LEVEL",
      "value": "2",
      "label": "二级(黄色)"
    }
  ],
  "timestamp": 1735689213000
}
```

***

## 9. 系统配置接口

### 9.1 获取系统配置

**路径：** `GET /api/v1/config/{code}`\
**描述：** 根据配置编码获取配置值

**成功响应：**

```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "code": "SYSTEM_NAME",
    "value": "地质灾害监测预警系统",
    "description": "系统名称",
    "type": "string"
  },
  "timestamp": 1735689213000
}
```

### 9.2 更新系统配置

**路径：** `PUT /api/v1/config/{code}`\
**描述：** 更新系统配置值

**请求体：**

```json
{
  "value": "地质灾害监测预警系统V2.0"
}
```

**成功响应：**

```json
{
  "code": 200,
  "msg": "更新成功",
  "data": null,
  "timestamp": 1735689213000
}
```

***

## 10. 文件上传接口

### 10.1 上传文件

**路径：** `POST /api/v1/upload`\
**描述：** 上传文件

**请求参数：**

| 参数   | 类型            | 必填 | 说明                   |
| ---- | ------------- | -- | -------------------- |
| file | MultipartFile | 是  | 文件                   |
| type | string        | 否  | 文件类型：image/doc/excel |

**成功响应：**

```json
{
  "code": 200,
  "msg": "上传成功",
  "data": {
    "url": "/uploads/20240120/test.png",
    "fileName": "test.png",
    "size": 102400
  },
  "timestamp": 1735689213000
}
```

***

## 11. WebSocket 实时推送

### 11.1 告警实时推送

**连接地址：** `ws://{host}/api/v1/ws/alarm`\
**描述：** 实时接收告警推送

**推送格式：**

```json
{
  "type": "ALARM",
  "data": {
    "id": 1,
    "hazardPointId": 1,
    "hazardPointName": "隐患点A",
    "alarmLevel": 2,
    "alarmLevelText": "二级(黄色)",
    "alarmMessage": "水位超出警戒水位",
    "createTime": "2024-01-20 14:30:00"
  }
}
```

### 11.2 设备状态推送

**连接地址：** `ws://{host}/api/v1/ws/device-status`\
**描述：** 实时接收设备状态变化

**推送格式：**

```json
{
  "type": "DEVICE_STATUS",
  "data": {
    "deviceId": 1,
    "deviceName": "水位雷达",
    "oldStatus": 1,
    "newStatus": 3,
    "statusText": "离线",
    "changeTime": "2024-01-20 14:30:00"
  }
}
```

***

## 附录：状态码对照表

| 状态码 | 含义          | HTTP状态码 |
| --- | ----------- | ------- |
| 200 | 成功          | 200     |
| 400 | 参数错误        | 400     |
| 401 | 未登录/Token过期 | 401     |
| 403 | 无权限         | 403     |
| 404 | 资源不存在       | 404     |
| 405 | 请求方法不支持     | 405     |
| 409 | 资源冲突        | 409     |
| 415 | 请求内容类型不支持   | 415     |
| 500 | 服务器异常       | 500     |
| 601 | 预警触发        | 200     |
| 602 | 设备离线        | 200     |

***

**文档版本：** v1.5\
**生成日期：** 2026-06-03\
**最后更新：** 2026-06-03\
**更新内容：** 新增 2.7 系统监控模块接口（MQTT 统计/监听器/配置/客户端管理/批量踢出），新增 2.6.7 监控总览接口，标注旧
`/sys/v1/monitor/*` 路径计划移除。
