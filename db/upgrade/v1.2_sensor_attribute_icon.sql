-- ===============================================================
-- v1.2 传感器属性表新增 icon 字段
-- ===============================================================
-- 执行时间: 2026-05-22
-- 执行原因: 支持传感器级别的图标配置，用于设备隐患点绑定功能
-- ===============================================================

ALTER TABLE `sensor_attribute`
ADD COLUMN `icon` VARCHAR(500) DEFAULT NULL COMMENT '图标路径' AFTER `range_max`;