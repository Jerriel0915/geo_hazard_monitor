<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwei.log.infrastructure.persistence.mysql.LogStreamCheckpointMapper">

    <insert id="upsert" parameterType="com.zwei.log.domain.model.LogStreamCheckpoint" useGeneratedKeys="true" keyProperty="id">
        insert into log_stream_checkpoint
        (subscriber_key, last_event_id, log_type, updated_at)
        values
        (#{subscriberKey}, #{lastEventId}, #{logType}, now(3))
        on duplicate key update
        last_event_id = values(last_event_id),
        updated_at = now(3)
    </insert>

    <select id="selectBySubscriberAndTypes" resultType="com.zwei.log.domain.model.LogStreamCheckpoint">
        select id, subscriber_key as subscriberKey, last_event_id as lastEventId, log_type as logType, updated_at as updatedAt
        from log_stream_checkpoint
        where subscriber_key = #{subscriberKey}
          and log_type in
        <foreach collection="logTypes" item="logType" open="(" separator="," close=")">
            #{logType}
        </foreach>
    </select>
</mapper>
