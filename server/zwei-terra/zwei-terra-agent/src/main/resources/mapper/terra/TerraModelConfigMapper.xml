<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwei.terra.agent.mapper.TerraModelConfigMapper">

    <resultMap type="com.zwei.terra.agent.domain.TerraModelConfig" id="TerraModelConfigResult">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="baseUrl" column="base_url"/>
        <result property="apiKey" column="api_key"/>
        <result property="modelName" column="model_name"/>
        <result property="maxTokens" column="max_tokens"/>
        <result property="temperature" column="temperature"/>
        <result property="isActive" column="is_active"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT id, name, base_url, api_key, model_name, max_tokens, temperature,
               is_active, sort_order, del_flag, create_by, create_time, update_by, update_time, remark
        FROM terra_model_config
    </sql>

    <select id="selectList" parameterType="com.zwei.terra.agent.domain.TerraModelConfig"
            resultMap="TerraModelConfigResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="isActive != null">AND is_active = #{isActive}</if>
        </where>
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectById" parameterType="Long" resultMap="TerraModelConfigResult">
        <include refid="selectVo"/>
        WHERE id = #{id} AND del_flag = '0'
    </select>

    <select id="selectActive" resultMap="TerraModelConfigResult">
        <include refid="selectVo"/>
        WHERE del_flag = '0' AND is_active = 1
        ORDER BY sort_order ASC
        LIMIT 1
    </select>

    <select id="checkNameUnique" resultMap="TerraModelConfigResult">
        <include refid="selectVo"/>
        WHERE del_flag = '0' AND name = #{name}
        <if test="id != null">AND id != #{id}</if>
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.zwei.terra.agent.domain.TerraModelConfig"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO terra_model_config (name, base_url, api_key, model_name, max_tokens,
                                        temperature, is_active, sort_order, del_flag,
                                        create_by, create_time, remark)
        VALUES (#{name}, #{baseUrl}, #{apiKey}, #{modelName}, #{maxTokens},
                #{temperature}, #{isActive}, #{sortOrder}, '0',
                #{createBy}, #{createTime}, #{remark})
    </insert>

    <update id="update" parameterType="com.zwei.terra.agent.domain.TerraModelConfig">
        UPDATE terra_model_config
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="baseUrl != null">base_url = #{baseUrl},</if>
            <if test="apiKey != null">api_key = #{apiKey},</if>
            <if test="modelName != null and modelName != ''">model_name = #{modelName},</if>
            <if test="maxTokens != null">max_tokens = #{maxTokens},</if>
            <if test="temperature != null">temperature = #{temperature},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_by = #{updateBy},
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="deactivateAll">
        UPDATE terra_model_config
        SET is_active = 0
        WHERE del_flag = '0'
    </update>

    <update id="deleteById" parameterType="Long">
        UPDATE terra_model_config
        SET del_flag = '1'
        WHERE id = #{id}
    </update>

</mapper>
