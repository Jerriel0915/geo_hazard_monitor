<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>zwei</artifactId>
        <groupId>com.zwei</groupId>
        <version>3.9.2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>zwei-iot-alarm</artifactId>
    <description>IoT告警中心 — 告警判据/综合策略/告警引擎/通知分发</description>

    <dependencies>
        <!-- 通用模块 -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-common</artifactId>
        </dependency>
        <!-- IoT 设备模块（跨模块 Service 接口） -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-iot-device</artifactId>
        </dependency>
        <!-- 监测字典（MonitorType/MonitorContent） -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-iot-monitor</artifactId>
        </dependency>
        <!-- 时序数据查询（IoTDB） -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-iot-timeseries</artifactId>
        </dependency>
        <!-- 隐患点 -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-iot-hazard</artifactId>
        </dependency>
        <!-- 框架核心（TokenService / Security 工具） -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-framework</artifactId>
        </dependency>
        <!-- RBAC 服务（角色/部门/用户查询，用于通知接收人选项） -->
        <dependency>
            <groupId>com.zwei</groupId>
            <artifactId>zwei-system</artifactId>
        </dependency>
        <!-- MyBatis -->
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
        </dependency>
        <!-- Spring Web (SSE) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Groovy (综合策略脚本引擎) -->
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy</artifactId>
        </dependency>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- 阿里云短信 SDK -->
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>dysmsapi20170525</artifactId>
            <version>3.1.0</version>
        </dependency>
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>tea-openapi</artifactId>
            <version>0.3.6</version>
        </dependency>
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>tea</artifactId>
            <version>1.3.3</version>
        </dependency>
        <!-- SMTP 邮件 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <!-- Thymeleaf 模板渲染（邮件 HTML）- starter 提供 TemplateEngine 自动配置 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <!-- Quartz (综合告警策略 CRON 动态调度) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-quartz</artifactId>
        </dependency>
    </dependencies>
</project>
