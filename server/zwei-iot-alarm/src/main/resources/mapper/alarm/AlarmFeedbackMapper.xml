<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwei.iot.alarm.mapper.AlarmFeedbackMapper">

    <resultMap type="com.zwei.iot.alarm.domain.AlarmFeedback" id="AlarmFeedbackResult">
        <id property="id" column="id"/>
        <result property="alarmId" column="alarm_id"/>
        <result property="content" column="content"/>
        <result property="files" column="files"/>
        <result property="operator" column="operator"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insertFeedback" parameterType="com.zwei.iot.alarm.domain.AlarmFeedback"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_feedback (alarm_id, content, files, operator, create_time)
        VALUES (#{alarmId}, #{content}, #{files}, #{operator}, #{createTime})
    </insert>

    <select id="selectByAlarmId" parameterType="Long" resultMap="AlarmFeedbackResult">
        SELECT id, alarm_id, content, files, operator, create_time
        FROM alarm_feedback
        WHERE alarm_id = #{alarmId}
        ORDER BY create_time DESC
    </select>

</mapper>
