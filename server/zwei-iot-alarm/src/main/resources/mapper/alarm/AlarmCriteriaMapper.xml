<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwei.iot.alarm.mapper.AlarmCriteriaMapper">

    <resultMap type="com.zwei.iot.alarm.domain.AlarmCriteria" id="AlarmCriteriaResult">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="monitorTypeId" column="monitor_type_id"/>
        <result property="monitorTypeName" column="monitor_type_name"/>
        <result property="monitorContentId" column="monitor_content_id"/>
        <result property="monitorContentCode" column="monitor_content_code"/>
        <result property="hazardPointId" column="hazard_point_id"/>
        <result property="levelConfig" column="level_config"/>
        <result property="persistCount" column="persist_count"/>
        <result property="silencePeriod" column="silence_period"/>
        <result property="isEnabled" column="is_enabled"/>
        <result property="version" column="version"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="selectVo">
        SELECT id,
               name,
               monitor_type_id,
               monitor_type_name,
               monitor_content_id,
               monitor_content_code,
               hazard_point_id,
               level_config,
               persist_count,
               silence_period,
               is_enabled,
               version,
               del_flag,
               create_by,
               create_time,
               update_by,
               update_time
        FROM alarm_criteria
    </sql>

    <select id="selectCriteriaList" parameterType="com.zwei.iot.alarm.domain.AlarmCriteria"
            resultMap="AlarmCriteriaResult">
        <include refid="selectVo"/>
        <where>
            del_flag = 0
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="monitorTypeId != null">AND monitor_type_id = #{monitorTypeId}</if>
            <if test="monitorContentId != null">AND monitor_content_id = #{monitorContentId}</if>
            <if test="hazardPointId != null">AND hazard_point_id = #{hazardPointId}</if>
            <if test="isEnabled != null">AND is_enabled = #{isEnabled}</if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectEnabledByMonitorContentId" resultMap="AlarmCriteriaResult">
        <include refid="selectVo"/>
        WHERE del_flag = 0 AND is_enabled = 1 AND monitor_content_id = #{monitorContentId}
    </select>

    <select id="selectEnabledByHazardPointId" resultMap="AlarmCriteriaResult">
        <include refid="selectVo"/>
        WHERE del_flag = 0 AND is_enabled = 1 AND hazard_point_id = #{hazardPointId}
    </select>

    <!-- 按监测类型加载兜底判据 (hazard_point_id IS NULL, 全局生效) -->
    <select id="selectEnabledByMonitorTypeId" resultMap="AlarmCriteriaResult">
        <include refid="selectVo"/>
        WHERE del_flag = 0 AND is_enabled = 1
        AND monitor_type_id = #{monitorTypeId}
        AND hazard_point_id IS NULL
    </select>

    <select id="selectAllEnabled" resultMap="AlarmCriteriaResult">
        <include refid="selectVo"/>
        WHERE del_flag = 0 AND is_enabled = 1
    </select>

    <select id="selectCriteriaById" parameterType="Long" resultMap="AlarmCriteriaResult">
        <include refid="selectVo"/>
        WHERE id = #{id} AND del_flag = 0
    </select>

    <insert id="insertCriteria" parameterType="com.zwei.iot.alarm.domain.AlarmCriteria"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_criteria (name, monitor_type_id, monitor_type_name,
                                    monitor_content_id, monitor_content_code, hazard_point_id,
                                    level_config, persist_count, silence_period,
                                    is_enabled, version, create_by, create_time)
        VALUES (#{name}, #{monitorTypeId}, #{monitorTypeName},
                #{monitorContentId}, #{monitorContentCode}, #{hazardPointId},
                IF(#{levelConfig} = '' OR #{levelConfig} IS NULL, '{}', #{levelConfig}),
                #{persistCount}, #{silencePeriod},
                #{isEnabled}, #{version}, #{createBy}, #{createTime})
    </insert>

    <update id="updateCriteria" parameterType="com.zwei.iot.alarm.domain.AlarmCriteria">
        UPDATE alarm_criteria
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="monitorTypeId != null">monitor_type_id = #{monitorTypeId},</if>
            <if test="monitorTypeName != null">monitor_type_name = #{monitorTypeName},</if>
            <if test="monitorContentId != null">monitor_content_id = #{monitorContentId},</if>
            <if test="monitorContentCode != null">monitor_content_code = #{monitorContentCode},</if>
            <if test="hazardPointId != null">hazard_point_id = #{hazardPointId},</if>
            <if test="levelConfig != null">level_config = #{levelConfig},</if>
            <if test="persistCount != null">persist_count = #{persistCount},</if>
            <if test="silencePeriod != null">silence_period = #{silencePeriod},</if>
            <if test="isEnabled != null">is_enabled = #{isEnabled},</if>
            version = version + 1,
            update_by = #{updateBy}, update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteCriteriaById" parameterType="Long">
        UPDATE alarm_criteria
        SET del_flag = 1
        WHERE id = #{id}
    </update>

</mapper>
