<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwei.iot.device.mapper.DeviceAuthLogMapper">

    <insert id="insert" parameterType="com.zwei.iot.device.domain.DeviceAuthLog" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO device_auth_log(
            device_id,
            auth_username,
            auth_result,
            client_id,
            client_ip,
            failure_reason,
            create_time
        ) VALUES (
            #{deviceId},
            #{authUsername},
            #{authResult},
            #{clientId},
            #{clientIp},
            #{failureReason},
            SYSDATE()
        )
    </insert>
</mapper>
